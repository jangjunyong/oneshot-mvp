@AGENTS.md

# 프로젝트

공시 근거로 보유 종목 사이의 관계를 캐는 에이전트. 3종목 이상 보유한 개인투자자가 "내 포트폴리오가 실제로 몇 덩어리인지" 확인한다.
지금은 **2주 MVP다. 프로덕션이 아니다.** 왜·누구·안 만들 것은 `PRD.md`.

# 실행

```
npm run dev        개발 서버 localhost:3000
npm run typecheck  타입 에러 0 확인
npm run lint       린트
npm run build      프로덕션 빌드
```

# 스택 (임의 변경 금지)

Next.js 16 (App Router) + TypeScript + Tailwind 4 · 배포 Vercel · DB 없음
**Next 16은 학습 데이터와 다르다.** API를 쓰기 전에 `node_modules/next/dist/docs/` 의 해당 가이드를 읽을 것.

# 규칙

- 새 의존성 추가 전 반드시 질문할 것
- 커밋은 기능 단위 하나씩
- `any` 금지 / 주석은 "왜"만 적을 것
- 문서·주석·UI 문구는 한국어
- DART API 키는 `.env.local`. 커밋 금지

# 이 프로젝트의 암묵지

- **근거 없는 간선은 그리지 않는다.** 공시 필드로 짚을 수 없으면 화면에 안 나온다. 추측을 그리는 순간 제품 가치가 0이다
- **🔴 동일산업 간선은 보여주되 클러스터 판정에서 뺀다.** 판정에 넣으면 "반도체 2종목이 한 덩어리"라는 당연한 소리만 나온다
- **"관계 없음"은 실패가 아니라 결과다.** 확인한 범위를 같이 내놓는다. 빈 화면은 답이 아니다

# 하지 말 것

- 근거 없는 간선 생성 — 어떤 이유로도
- 타입 에러·린트를 `ignore`·`skip`으로 덮어 통과시키기
- `.env*` 커밋
- 요청하지 않은 리팩토링
- `PRD.md`의 "안 만들 것" 목록에 손대기
