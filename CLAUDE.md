@AGENTS.md

# 프로젝트

축제 기획안을 넣으면 **닮은 과거 축제들이 실제로 어떻게 무너졌는지**를 근거로 경보 등급과 보완점을 내는 웹서비스. 사용자는 축제를 기획하는 실무자다.
지금은 **2주 MVP다. 프로덕션이 아니다.** 왜·누구·안 만들 것은 `PRD.md`.

# 실행

```
npm run dev        개발 서버 localhost:3000
npm run typecheck  타입 에러 0 확인
npm run lint       린트
npm run build      프로덕션 빌드
```

# 스택 (임의 변경 금지)

Next.js 16 (App Router) + TypeScript + Tailwind 4 · 배포 Vercel · **DB 없음**
축제 데이터는 `data/` 아래 정적 JSON. API는 Next.js Route Handler.
**Next 16은 학습 데이터와 다르다.** API를 쓰기 전에 `node_modules/next/dist/docs/` 의 해당 가이드를 읽을 것.

# 규칙

- 새 의존성 추가 전 반드시 질문할 것
- 커밋은 기능 단위 하나씩
- `any` 금지 / 주석은 "왜"만 적을 것
- 문서·주석·UI 문구는 한국어
- API 키는 `.env.local`. 커밋 금지

# 이 프로젝트의 암묵지

- **숫자를 예언하지 않는다.** "몇 명 온다"를 출력하는 순간 이 제품은 틀릴 수 있는 예측기가 된다. 과거 축제의 실제 기록만 보여준다
- **왜 닮았는지를 항상 같이 낸다.** 유사도 점수만 던지면 실무자는 못 믿는다. 지역·시기·테마·규모 중 무엇이 닮았는지 항목으로 보여야 한다
- **"안전합니다"를 말하지 않는다.** 근거를 못 찾은 것과 안전한 것은 다르다. 근거가 없으면 "판단 못 함"이다

# UI/UX 스타일 가이드

실무자가 **회의 중에 화면을 띄운다.** 훑는 화면이 아니라 근거를 대는 화면이다.

- **결론이 맨 위, 근거는 접어둔다.** 경보 등급과 한 문장이 먼저 보이고, "왜 닮았나"·"그들이 겪은 것"은 눌러야 펼쳐진다
- **경보 등급은 색만으로 구분하지 않는다.** 색 + 텍스트 + 명도 차이를 함께 쓴다. 빨강/노랑만으로 가르면 못 읽는 사람이 있다
- **숫자는 `tabular-nums`.** 예상 대비 실제를 세로로 비교하는 화면이라 자릿수가 흔들리면 안 된다
- **출처를 숨기지 않는다.** 모든 과거 기록에 출처와 연도를 붙인다. 출처 없는 숫자는 화면에 올리지 않는다
- 폰트는 시스템 한글 폰트, 본문 15~16px. 회의실 프로젝터에서 읽히는 크기
- 애니메이션은 넣지 않는다. 판단을 돕지 않는다

# 하지 말 것

- 방문객 수 예측값 생성 — 어떤 이유로도
- 출처 없는 축제 기록을 화면에 올리기
- 타입 에러·린트를 `ignore`·`skip`으로 덮어 통과시키기
- `.env*` 커밋
- 요청하지 않은 리팩토링
- `PRD.md`의 "안 만들 것" 목록에 손대기
