# PLAN — 2주를 자른 결과

> 수직 슬라이스로 자른다. DB → API → UI 순서가 아니라 **한 기능을 끝까지** 만든다.
> 매일 배포한다. Day 11부터 기능 동결.

## Day 7 공개 선언

> **"김천김밥축제 1회 조건을 넣으면 '닮은 축제 3곳이 평소의 2.4~3.1배가 왔습니다'가 뜨는 걸 보여준다."**

## 멘토링 3회차 (제출 양식 기준)

| 일자 | 목표 단계 | 세부 작업 | 완료 |
|---|---|---|---|
| **8/22 (1차)** | 기획 & 환경 설정 | 주제 선정 · PRD/WBS 작성 · CLAUDE.md 정의 · GitHub 저장소 + Vercel 배포 + CI 구축 | ✅ |
| **8/23 (2차)** | 핵심 기능 구현 | 데이터 검수(619건) · 특징 벡터 실측 확정 · PRD 정합화 · F1~F3 배포 3회 | ⬜ |
| **8/29 (3차)** | 연동 & 배포 | LLM 기획안 파싱 · 경보 등급/근거 화면 · 최종 배포 | ⬜ |

## 14일 일정

| Day | 날짜 | 산출물 | 핵심 |
|---|---|---|---|
| 1 | 8/22 (토) | 산출물 5종 · **배포 URL** | **1차 멘토링.** 실제 사람 1명에게 PRD 확인 |
| 2 | 8/23 (일) | 데이터 검수 · 매칭 특징 확정 | **2차 멘토링.** 619건 확보 완료. 어떤 특징으로 닮음을 잴지 결정 |
| 3–4 | 8/24 (월)–8/25 (화) | **슬라이스 1** | 폼 입력 → 닮은 축제 3개 → 화면 |
| 5–6 | 8/26 (수)–8/27 (목) | **슬라이스 2 · 3** | 경보 등급 · 근거 열람 |
| 7 | 8/28 (금) | 실사용자 3명 피드백 | 중간 체크포인트 |
| 8 | 8/29 (토) | 슬라이스 4 착수 | **3차 멘토링 — 연동 & 배포** |
| 9–10 | 8/30 (일)–8/31 (월) | 슬라이스 4 · 피드백 반영 | **새 기능 추가 없음** |
| 11–12 | 9/1 (화)–9/2 (수) | 버그 수정 · 화면 정리 | **기능 동결** |
| 13–14 | 9/3 (목)–9/4 (금) | 데모 리허설 | **3분 안에 끝나야 한다** |

## 오늘 (Day 2) — feature 3개, 배포 3회

한 feature = 입력부터 화면까지 한 줄 관통. 끝나면 **배포하고** 커밋.
먼저 `lib/types.ts` 에 `PlanInput`/`MatchedFestival`/`MatchResult`/`AlertGrade` 를 main에 커밋한다 — 이게 있어야 F2·F3가 파일 충돌 없이 병렬로 돈다.

| | feature | 브랜치 | 건드리는 파일 |
|---|---|---|---|
| **F1** | 입력·결과 뼈대 | `feat/skeleton` | `app/page.tsx` · `app/result/page.tsx` |
| **F2** | 닮은 축제 매칭 | `feat/matching` | `lib/festivals.ts` · `lib/match.ts` · `app/api/match/route.ts` |
| **F3** | 경보 등급 + 근거 | `feat/alert-grade` | `lib/grade.ts` · `app/result/page.tsx` 연결 |

### F1 — 스켈레톤 (와이어프레임)

스토리: **기획안을 붙여넣는다 → 뽑힌 항목을 확인한다 → 경보를 본다.**

```
[app/page.tsx]                          [app/result/page.tsx]
기획안 붙여넣기 (textarea)                ⚠ 경보: 심각
       [ 항목 뽑기 ]                     닮은 축제 3곳이 평소의 N배가 왔습니다
─────────────────────                   ▸ 왜 닮았나        (접힘)
뽑힌 항목 — 틀리면 고쳐 주세요             ▸ 그들이 겪은 것    (접힘)
 지역[시도▾][시군구▾] 시기[월▾] 테마[8종▾]
 인구[자동] 접근성[자동]
       [ 닮은 축제 찾기 ]
```

- **"항목 뽑기"는 오늘 파싱하지 않는다.** 안내만 띄운다 — Day 8에 LLM 붙일 자리를 지금 잡아둔다
- 전부 하드코딩 더미. 데이터·API 안 붙임
- 완료 조건: 배포 URL에서 폼 제출 → 결과 화면 이동 · eval **05**

### F2 — 닮은 축제 매칭 (순수 함수, LLM 없음)

| 축 | 계산 | 가중치 |
|---|---|---|
| 접근성 | `\|a1-a2\| / 4` | 0.30 |
| 인구 | `min(1, \|log p1 - log p2\| / log 10)` | 0.30 |
| 지역 | `min(1, haversine_km / 300)` | 0.20 |
| 시기 | 월 원형거리 `min(\|m1-m2\|, 12-\|m1-m2\|) / 6` | 0.10 |
| 테마 | 코드 일치 0 / 불일치 1 | 0.10 |

- 가중합 **거리 > 0.35 이면 제외** (김천 조건으로 검증 후 조정)
- **`preSearchSurge` 재유입 금지** — 59%가 아티팩트
- **무엇이 닮았는지 축별로 표시** (암묵지 2번)
- 완료 조건: eval **01 · 02 · 08 · 14**

### F3 — 경보 등급 + 근거 ★ 제품의 심장

- 닮은 3곳의 **중앙 배수** → 2.00↑ 심각 / 1.50~2.00 주의 / 미만 **"위험 근거 못 찾음"**
- **"안전합니다"를 출력하지 않는다**
- 모든 기록에 **연도 + 출처**. 배수는 `tabular-nums`. 등급은 색+텍스트+명도
- 완료 조건: eval **03 · 04 · 06 · 07 · 12 · 15**

### feature별 CLAUDE.md

루트 `CLAUDE.md` 를 브랜치마다 고치면 병합 때 충돌한다. **디렉터리 단위**로 둔다.

| 파일 | 담는 것 |
|---|---|
| `app/CLAUDE.md` | 결론 위·근거 접힘 · 색+텍스트+명도 · `tabular-nums` · 애니메이션 금지 · **로직 금지** |
| `lib/CLAUDE.md` | 5축 가중치 · `preSearchSurge` 금지 · **순수 함수** · 예측값 생성 금지 |
| `app/result/CLAUDE.md` | 컷 2.0/1.5 · **"안전" 문구 금지** · 출처·연도 없는 숫자 금지 |

### 워크트리

F1 → main 병합 후, F2·F3는 파일이 안 겹치므로 병렬. **볼트 밖에** 만든다 (볼트 안에 `node_modules` 들어가면 Obsidian 색인이 터진다).

```
git worktree add C:/Users/jacob/oneshot-ws/matching -b feat/matching
git worktree add C:/Users/jacob/oneshot-ws/grade    -b feat/alert-grade
```

### 오늘 안 만들 것

- 로그인 · 회원가입 · 결제 · 관리자 · 알림 · 다크모드
- **DB** — 정적 JSON
- **LLM 호출** — 오늘 3개는 전부 순수 함수
- **방문객 수 예측값 · 예상치 입력란** — 영구 금지
- 지도 시각화 · 붕괴 서사(소진·민원) · 테스트 프레임워크 도입
- 요청하지 않은 리팩토링

## 슬라이스 4 — 기획안 붙여넣기 + 실패 경로 (Day 8–10)

- `app/page.tsx` 의 **"항목 뽑기"에 LLM 파싱을 붙인다** (화면은 F1에서 이미 만들어 둠)
- 항목이 빠졌으면 무엇이 없는지 짚어 되묻는다
- eval **09 · 10 · 11** 통과 (09는 붕괴 서사를 손수 붙인 뒤라야 재현된다)
- ★ **에이전트/워크플로우 판정** — 입력마다 보는 위험축이 달라지는지 재고 `docs/FLOW.md` §1에 숫자로 덮어쓴다

## 리스크

| 리스크 | 대응 |
|---|---|
| ✅ ~~"실제로 몇 명 왔나" 데이터가 없다~~ **— 해소됨 (8/23)** | 한국관광 데이터랩 `locgoRegnVisitrDDList` 로 **619개 축제의 평소 대비 방문 배수를 이미 확보.** 손수집 불필요 |
| 🔴 **`preSearchSurge` 오염** | 1.00 이 264건(43%), 10.00 이 98건(16%) — **59%가 아티팩트.** 특징 벡터에서 제거함. 사전 검색 신호를 쓰려면 원자료부터 다시 |
| 실제 사용자를 못 만난다 | 지자체 실무자는 어렵다. **대학 축제 기획자**로 Day 7 테스트 |
| LLM이 장식이 된다 | 슬라이스 1~3은 함수임을 인정하고 시작. 슬라이스 4에서 판정 |
| 닮은 축제가 늘 같은 것만 나온다 | Day 5~6에 다양한 입력으로 확인. 편중되면 특징 가중치 조정 |
| 🟡 **심각 등급이 한 번도 안 뜰 수 있다** | p90이 1.84라 3곳 중앙 2.0을 넘기가 드물다. **김천 조건으로 실측하고 안 뜨면 컷라인을 재검토한다** — 우기지 않는다 |
| 🟡 **결과 필드가 배수 하나뿐** | "그들이 겪은 것"이 한 줄로 얇다. Day 8에 상위 몇 건만 언론 보도로 보강 |
| 테마 코드표가 없다 | 코드 3에 59%가 몰림. 가중치 0.10 으로 낮추고, 원본 분류표를 구하면 다시 본다 |
| 일정 충돌 — 9/4 동작구 발표 | Day 13–14 리허설을 9/3에 당겨 끝낸다 |

## 규칙

- 커밋 하나 = 지시 하나 = 되돌리기 지점 하나
- 매일 배포. 배포를 마지막 주로 미루면 절반이 무너진다
- Day 11부터 **새 기능 없음.** 데모에 없을 기능은 만들지 않는다
