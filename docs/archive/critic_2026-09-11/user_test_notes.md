# 실사용 관찰 기록 — 2026-09-11 (배포본 https://oneshot-mvp.vercel.app, 크롬 확장 + curl)

## 경로 1. 실제 기획서 PDF (정선 고한 오즈로드 축제행사 계획, 3쪽, 3,510자) → `/` 업로드 → 추출
- 결과: 예상 방문객 못 찾음(문서에 없음, 정당) · 개최 기간 못 찾음(문서엔 "2022 5~11월"만, 정당) · **시도·시군구 못 찾음** · 개최 월 못 찾음 · 접근성·인구 못 찾음 · 예산 9,000만 원(정확, 90,000천원 환산 맞음)
- **문서 본문에 "강원도 정선군 탄탄대로 사업(정선군)"과 "고한읍"이 명시돼 있는데 시도·시군구가 비었다.** 추출기가 "개최장소" 형식이 아니면 지역을 못 잡는다. 담당자는 이 칸을 다시 타이핑해야 한다
- 제출 후 `/?draft=17` 로 돌아오는데 **화면이 맨 위(히어로)** 다. SECTION B(뽑은 항목)는 스크롤해야 보인다. 처음 쓰는 사람은 "아무 일도 안 일어났다"고 느낀다
- 다리 링크 → `/check?sido=&sigungu=&budget=9000` — 이름·기간 없이 예산만 넘어간다. `/check`는 견본(군포) 판정을 그대로 보여 준다(시도·시군구가 비면 견본으로 폴백). **PDF를 올린 사람이 군포 판정을 보게 된다**
- draft id 가 17·18 로 숫자 증가 → Neon DB 경로. 누구나 `/?draft=1` ~ 을 열어 남의 초안을 볼 수 있다(로그인 없음)

## 경로 2. 붙여넣기 텍스트(문경 사과축제 가상 기획, 294자) → 추출
- 잘 뽑힘: 350,000명·기간 총계·연인원 / 2027-10-15~24 / 주차 1,200면·부스 80·예산 120,000만 원 / 시도·시군구 "경상북도 문경시" / 월·테마·접근성 근거 문장
- **본문에 "지난 회차: 2026.10.16~10.25"와 "2026년 방문객 28만 명"이 있는데 이력 기간으로 안 넘어간다** ("지난 회차 날짜는 판정 화면에서 적는다"). 담당자가 `/check`에서 다시 타이핑
- 추출 소요 ~15초. 진행 표시 없음(버튼 누른 뒤 정적)

## 경로 3. `/check` 다른 축제 — 보령머드축제 2027 (충남 보령시, 200만·기간 총계·연인원, 예산 40억, 이력 2024-07-19~08-04 / 2025-07-25~08-10 / 2026-07-24~08-09)
- 200, 1.7초. 판정 **과대**(1단계 0.77 과대 / 2단계 5.16 주의 신호 / 3단계 r 1.66 과대). 자기 이력 평균 1.59·1.77·1.85, 최대일 2.75·2.81·2.90. 내년 구간 평균 1.5~1.9 · 최대일 2.7~3.0. 또래 "인구 6~12만 95곳"
- **버그: 귀속 경고가 자기 자신을 경쟁 축제로 센다** — "반경 50km 안에서 다른 축제 1건이 같이 열렸다: 보령머드축제(0km)". `/check`·`/evidence`·`/report/check` 세 화면 모두. `lib/overlap.ts:148-154` 에 이름 제외 필터가 없다. 군포 견본의 "31건 외 28곳"에도 군포철쭉축제 자신이 들어 있을 가능성
- **2026 이력 끝(08-09)이 자료 끝(2026-08-09)과 같아 "+4주" 창이 통째로 없다.** `/evidence` 2026 곡선 캡션에만 "창 안에 빠진 날 28일"이 적히고, `/check` 판정표·`/report/check` 에는 안 적힌다. 보고서 2장 머리는 "빠진 날 0일"이라고 쓴다(적재 전체 기준이라 틀린 말은 아니지만 이 판정의 평소값은 전 4주만으로 계산됐다). 2026 배수 1.85 가 세 해 중 최대이고 그 값이 3단계 분모(이력 최대)와 1·2단계 분모(작년)를 정한다 → 판정 결과에 직접 영향
- "시군구 44180" 같은 코드 노출은 출처 표기로 좋다
- 이력 날짜를 담당자가 정확히 알아야 한다. 심사위원은 모른다 → 웹검색으로 찾아야 했다(보령 2025 일정 검색 2회). 자동 탐지 금지 규칙은 이해하나, TourAPI `searchFestival2`로 **올해 기간만이라도** 채워 주는 버튼이 없다

## 경로 4. `/check` 입력 변형 (curl)
- 시도 "충청남도"·시군구 "보령" → **"시군구 코드를 찾지 못했다. 행정 이름 그대로(군포시·청주시 상당구) 적어 달라"** → 판정 없음. 정식 명칭·약칭 정규화 없음("충남"만 됨). 심사위원이 첫 시도에서 막힐 확률 높음
- 이력 2026-08-20~08-30(자료 밖) → 뺐다는 문구는 있음("자료 밖이라 뺐다 ( no-festival-days ). 적재 범위 2019-05-01 ~ 2026-08-09") — 영문 코드 노출. 그런데 헤드라인은 **"첫 회 축제라 자기 이력이 없다"** 로 나간다(첫 회가 아니라 자료가 없는 것)
- 일 최다·실인원 100만 → 상한 초과, 회전율 표시 (동작)
- 서울 강남구 첫 회 5만 → 근거 없음 + 또래 구간만. 첫 회 축제는 항목 5개 전부 "근거 없음"·"신뢰도 낮음". 홈 카드 3은 "첫 회 축제는 닮은 축제 619건의 등급이 대신 선다"고 약속하는데 `/check`에는 619 등급이 안 나온다(그건 `/` 진단에 있다)

## 경로 5. `/venue`
- 로드 OK. 질문 "17번 부스에 줄이 길게 서면 통행에 방해가 되나요?" → 시나리오 번역 성공("[모델이 옮김]"), 90분 재생 20×(실시간 4.5분). **결과: "체험 17: 줄 최대 5, 최장 대기 2.4분 … 최대 10.00명/㎡. 줄이 통로를 먹는다."**
- **10.00명/㎡는 물리적으로 불가능한 값**(행안부 위험 5, Weidmann 상한 ~5.4). `lib/sim/sim.js:415` 주석이 같은 현상(부스 경계 겹침 20명/2㎡)을 09-09 에 "고쳤다"고 하나 질문 시나리오에서 재현. 심사위원이 보면 엔진 신뢰가 무너진다
- 재생 중 KPI "시각 00:00 · 장내 인원 0" 이 20초 넘게 그대로(재생 상태줄은 00:13/01:00). 화면 위 점도 안 보임 → "안 도는 것 아닌가" 오해
- 개발 흔적 노출: "격자 278×714 (1m) · 걸을 수 있는 면적 44190㎡ · 워커", "예전 캔버스 편집기 (Konva) · 위성 위 편집이 붙을 때까지 남겨 둔다", "난수 시드"
- 도면은 군포 1건 고정. 보령 판정에서 넘어와도 군포 도면

## 경로 6. 홈 `/` 문구
- 카드 2: "통과·주의·과대·**성립 불가** 중 하나와 내년 배수 구간" — M6 에서 폐기한 옛 라벨이 남아 있다. `app/page.tsx:210-211` 에서 "성립\n불가"로 줄이 갈라져 `copy.test` M6-3(문자열 0건)이 못 잡았다
- `app/_components/season-table.tsx:107` "물은 달" (V1 에서 고쳤다는 항목, `/report`쪽만 고침)
- "하루 45건까지" 쿼터 문구 노출(전역 카운터). "진단 이력 (0건) … 이 주소를 연 모든 사람이 같은 목록을 봅니다" — 정직하나 심사위원에게 "미완성"으로 읽힘
- SECTION A 다음 SECTION C (B 는 초안이 있을 때만) — 알려진 것
- 홈 TTFB 1.3~4.4초(콜드 스타트). `/check` 1.2~1.7초

## 경로 7. 모바일
- 창 리사이즈가 확장에서 안 먹어 실측 못 함. viewport meta 는 있음. CSS 브레이크포인트 34/44/60/64rem·720px·900px·1100px(venue) 존재. 이전 검증대로 `/venue`는 터치 핸들러 0건

## 경로 8. 크롬 확장 자체
- `/check`·`/evidence` 캡처가 30초 타임아웃 2회(렌더러 프리즈). 페이지 자체 문제인지 확장 문제인지 미확정 — 로컬 헤드리스로 재현 필요
