# 콘텐츠랩 제출 입력표 (마감 2026-09-21 16:00)

> 제출처: **한국관광 콘텐츠랩** https://conlab.visitkorea.or.kr/ → 로그인 → 공모전 접수확인 → 팀명/서비스명 선택 → 제출 페이지
> 계정: 공모전 참가 신청한 계정. 이메일 인증 필요(프로필은 투어라즈 touraz.kr 마이페이지에서 확인)
> **기한 내에는 접수 페이지에서 수정 가능. 16:00 이후 수정 불가.**

아래 값을 그대로 붙여 넣는다. 기능설명서 PDF 와 글자가 같아야 한다.

---

## 1. 팀 정보

| 칸 | 값 |
|---|---|
| 팀명 | **화면에 이미 있는 값(변경 불가)** — 그대로 확인만 |
| 팀 구성원 | 장준용 (숭실대학교 AI소프트웨어학부) · **1인 팀** |

## 2. 서비스 정보

**① 서비스명**
```
기획안 팩트체크 — 지자체 축제 예산 심의용 실측 검증
```

**② 서비스 개요**
```
축제 기획서(PDF)를 올리면 예상 방문객·개최 기간·예산을 옮겨 적고, 그 축제가 지난 회차에 실제로 겪은 한국관광공사 KT 시군구 일별 실측(외지인 배수·순증)과 같은 기준으로 재서 통과/주의/과대/과소/상한 초과/근거 없음을 판정합니다. 내년 배수 구간, 1인당 예산 대조, 같은 시기 경쟁 축제 귀속 경고, 결재 첨부용 검증 보고서 A4 2장을 내고, 행사장 도면 위 보행 시뮬레이션으로 쏠림이 어디서 막히는지를 미리 봅니다. 판정·시뮬에 생성형 모델을 쓰지 않습니다.
심사용 경로: 시연 안내 https://oneshot-mvp.vercel.app/judge-guide.pdf · 예비 데모 기획서 https://oneshot-mvp.vercel.app/sample-plan.pdf (첫 화면에 올리면 판정이 열립니다)
```

**③ 서비스 유형** → `웹`

**④ 선택 지정과제** → **9번 · 축제 흥행 예보 서비스**
> ⚠️ 중복 선택 불가. **과제번호 혼동 주의** — 9번이 맞는지 두 번 확인.

## 3. 서비스 테스트 정보

**① 서비스 URL**
```
https://oneshot-mvp.vercel.app
```

**② 테스트 계정** → **`로그인 불필요`** 선택
> 이 서비스는 로그인·설치·회원가입이 없다. 별도 계정을 만들 필요가 없다.
> 빈 화면으로 보이지 않게 개요 칸에 심사용 경로 두 개(시연 안내·예비 데모 기획서 PDF)를 넣어 뒀다.

## 4. 한국관광공사 OpenAPI 활용 정보

**① 인증키(인코딩·디코딩 일체)**
- data.go.kr 로그인 → 마이페이지 → 개발계정 상세 → **일반 인증키(Encoding)·(Decoding) 둘 다 복사**
- 이 키가 `서비스/.env.local` 의 `TOUR_API_KEY`(`eb1296…`)와 같은 키다. 다른 키를 내면 호출 이력이 안 잡힌다.
- **(선택) 운영계정 신청 — 체크 권장.** 대표 1개는 `DataLabService/locgoRegnVisitrDDList`
  (전수 적재 697,403행이 이 API 하나에서 나온다)

**② 활용한 공사 OpenAPI 2종**

| # | API |
|---|---|
| 1 | 한국관광공사 TourAPI 4.0 관광 데이터랩 서비스 — 지역별 방문자 수 일별 (`DataLabService/locgoRegnVisitrDDList`, KT 이동통신 기반) |
| 2 | 한국관광공사 TourAPI 4.0 국문 관광정보 서비스 — 축제 검색 (`searchFestival2`) |

> 기타 데이터 5종(데이터랩 축제 목록 619건 · 행안부 주민등록 인구 · OSM+공식 안내도 · 브이월드/Natural Earth · 월력요항)은
> 기능설명서 11쪽에 이미 적혀 있다. 콘텐츠랩 4번 칸에는 **공사 OpenAPI 2종만** 넣는다.

## 5. 기능설명서 (첨부)

```
서비스\docs\제출양식\기능설명서_기획안팩트체크_최종본.pdf
```
- 공식 양식 12쪽 · PDF · 그림 18장(전부 2026-09-20 도면 v3 화면) · 자리표시자 0
- 예비본: `기능설명서_기획안팩트체크_안전본.pdf` (그림 자리표시자 버전, 최종본 업로드 실패 시에만)

---

## 올리기 전 마지막 확인 (5분)

- [ ] **부문** — ②-2 **웹·앱 구현 부문** 맞는지 (개발 부문과 마감이 같아 오첨부 사고가 잦다)
- [ ] **지정과제 9번** 선택됐는지
- [ ] 첨부 파일이 `_최종본.pdf` 인지 (`_안전본`·`_old_2026-08-30` 아님)
- [ ] 서비스 URL 이 `https://oneshot-mvp.vercel.app` 인지 (배포별 해시 주소 아님)
- [ ] 인증키가 Vercel 환경변수·`.env.local` 과 같은 키인지
- [ ] 팀원 정보 최종 확정(1인)
- [ ] 제출 후 접수 페이지를 새로고침해 **저장된 값이 보이는지** 확인

## 제출 뒤

운영 주소 9개 경로가 살아 있는지 마감 직전에 한 번 더 본다(`제출본_배포기준.md`).
깨지면 코드를 고치지 말고 Vercel 에서 `06b6615` 배포를 Promote.
