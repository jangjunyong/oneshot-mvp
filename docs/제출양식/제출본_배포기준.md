# 제출본 배포 기준 (2026-09-20 갱신)

- 서면 캡처 기준 운영 배포: 커밋 **06b6615** (main, Vercel Production, https://oneshot-mvp.vercel.app)
  — 도면 v3(GitHub 결) 재구성본. 이전 기준은 bff0225(도면 v2)였다.
- 캡처 원본: `캡처/` 24장 — **손으로 찍지 않는다.** `node scripts/capture.mjs`
  (헤드리스 크롬 CDP, 요소 선택자, 의존성 0). 옛 캡처는 `캡처_2026-09-18/`에 그대로 둔다.
- 제출 파일: `기능설명서_기획안팩트체크_최종본.pdf` (12쪽, 흐름도 16장 + 대표 + 상세,
  `결과.txt` placeholders=0, `topdf.py` 검사 pages=12·해시태그·운영 주소 True)
- **다시 만드는 법 (세 줄)**
  ```
  npm run build
  node scripts/capture.mjs
  cd docs/제출양식 && python fill.py && python topdf.py
  ```
- 마감 전 점검: 운영이 06b6615 이후 커밋을 서빙하는지 → 첫 화면 · 군포 판정 ·
  첫 회 축제 · 화천 · `/evidence` · `/report/check` · `/venue` · PDF 3종 200 확인.
  깨지면 코드 수정 대신 Vercel 에서 06b6615 배포를 Promote.
- KT 적재는 2026-08-09까지(공사 자료가 08-19까지만 있어 연장 이득 없음, 2026-09-18 확인)
