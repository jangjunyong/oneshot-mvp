# /clear 후 붙여넣을 프롬프트

아래 블록을 그대로 복사해서 새 세션 첫 메시지로 넣으면 된다.
(2026-09-10 갱신 — 적대적 검증 뒤 "잔여 8일 밴드" 실행 회차, M0·M1 끝)

---

```
관광데이터 공모전 서비스 이어서 작업 (볼트 `대회/관광데이터/서비스/`).
먼저 docs/HANDOFF.md 맨 위 "2026-09-10" 절(사슬 진행표)을 읽고,
docs/verdict_2026-09-09_ralplan.md 의 §1.3(사슬)과 §4 의 M2~M7·M12 수용 기준을 읽어.
그다음 docs/DECISIONS.md 끝의 "2026-09-09 밤 3 → 09-10" 절. 08-31 기록은 `/`·`/report`·데이터·작업 규약에 아직 맞다.

## 지금 상태 (한 문단)

9/9 밤 ralplan 적대적 검증(Planner→Architect·Critic 4회차 APPROVE + 독립 레드팀)이 67점·상위 3 진출 13~19% 로 판정했고,
나는 잔여 8일 밴드(M0~M7 + 기능설명서 축약본, 실작업 5.6일)로 가기로 했다. M0(공사 유래 셀 비율 1.00, docs/measured_ratio.md)과
M1(구간 신뢰도 라벨 폐기, "이력 범위(N년). 적중률은 아직 재지 못했다")은 끝났고 커밋·push 됐다.
제품은 세 축: ① /check 기획안 판정 → /evidence → /report/check(A4 2장) ② / 진단 → /report(보조, 619건 재계산본) ③ /venue 시뮬.
유닛 259 · e2e 12 · 린트 0 오류 · typecheck·build 통과. Vercel 배포됨.

## 방침 (안 바뀐 것)

- 불문율 5개(HANDOFF "절대 어기지 않는 것"). 판정·시뮬에 LLM 0회. 새 의존성 0. lib/surge.ts 불변. 새 화면 0.
- 명·배·일 숫자는 `app/_components/num.tsx` 의 `<Num>` 으로만. e2e 가 출처 셀 밖 "N명" 을 센다.
- 검증 문서의 점수·확률 숫자는 제출물에 인용하지 않는다.
- 마디마다: 순수 함수 TDD → 화면 → `npm run lint && npm run typecheck && npm test && npm run build && npm run test:e2e` →
  **e2e 요약 줄(ℹ pass N)을 눈으로 읽은 뒤** 커밋 → push. 배경 검증 사슬을 grep 으로 끝내면 exit 0 이라 실패가 묻힌다(9/9 사고).
- 화면 문구: 산문에 em-dash 금지, 문장당 strong 하나, "안전" 단언 0.

## 이번 회차 할 일 — 사슬 순서대로, 되묻지 말고

M2 문구 통일(0.4일) → M3 1인당 예산 카드(0.5) → M4 로고·나침반(0.3) → M5 −52주 근사 백테스트(0.8) →
M6 counting 반영·손익분기 회전율·라벨 "상한 초과"(1.0) → M7 견본 2건(0.7) → M12 기능설명서 축약본(1.5).
각 마디의 기계 검사 기준은 verdict §4 그대로. 마디 하나 끝날 때마다 HANDOFF 진행표의 상태 칸을 갱신하고 커밋.
M6 은 판정 규칙 변경이라 완료조건 #13 회귀(양성 217,502 → 라벨 / 음성 110,184 → 통과)를 갱신하고, 음성이 "통과"를
못 지키면 되돌린다. M5 는 같은 축제 다년 표본이 0건이라 −52주 근사만 가능하다 — 근사·표본 N·편향 양방향 문구 셋을 다 적는다.

계획을 한 단락으로 선언하고 M2 부터 바로 시작해. 데모 먼저.

## 내가(사용자) 해야 하는 것 — 아직 안 했으면 상기시켜

- 사무국(070-4060-4046) 1차 심사자료 제출일 확인. 무회신이면 9/15부터 잔여 5일 밴드(M0~M4 + 축약본)로 축소.
- data.go.kr 에서 `AreaTarResDemService` 활용신청.

백그라운드에 dev 서버(localhost:3000)가 돌고 있을 수 있다 — `curl localhost:3000` 로 확인하고 없으면 `npm run dev`.
```
