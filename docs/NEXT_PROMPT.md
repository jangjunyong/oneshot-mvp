# /clear 뒤 붙여넣을 프롬프트 (2026-09-13 기준)

아래 블록을 그대로 붙여 넣는다.

---

`대회/관광데이터/서비스/docs/HANDOFF.md` 맨 위 절 "(2026-09-13) 견본 제거 · 기획서를 넣어야 열린다"와 `docs/DECISIONS.md` 2026-09-13 절을 먼저 읽어라. 마디마다 `npm run lint && npm run typecheck && npm test && npm run build && npm run test:e2e` 통과 → 커밋 → push → 배포 curl 확인.

1. **사이트에 견본을 되살리지 않는다.** 빈 URL 은 `empty` → 안내 화면. 데모는 `public/군포 철쭉축제 기획서.pdf`(scripts/sample-plan.mjs 생성)를 첫 화면에 올려서만 본다. `?demo=` 를 다시 만들지 않는다. 첫 화면에 문구·버튼을 넣지 않는다.
2. **지도 핀은 검정선**(기둥 + 원 머리, 이 기획안은 ⊕). 파란 그림 핀으로 되돌리지 않는다.
3. **시뮬레이션은 편집이 먼저**다. 자동 재생을 넣지 않는다. PDF 배치도는 밑그림으로만 깔고 자동 변환하지 않는다(B안은 실물 배치도 PDF 로 정확도를 잰 뒤 사용자 결정).
4. 제한·설계 판단이 사용자 기대와 어긋날 수 있으면 **작업 전에 이유를 말한다**: 사용자가 겪는 일 → 왜 → 대안 → 추천.
5. 잔여 S1: #4·#6·#11 고침, **#8 A안 끝**(연도마다 축제 신호 표시·판정 불변, `docs/축제신호_기준_2026-09-14.md`). 문턱을 바꾸면 `scripts/signal-eval.mjs` 로 다시 잰다 — 등급을 판정에 쓰거나 해를 이력에서 빼지 않는다(분별력 부족). #10 은 사용자가 브이월드에서 도메인을 걸면 `docs/vworld_키_도메인제한.md` 확인법으로 재측정.

지키는 것: 판정 경로 LLM 0회·결정론(CHECK_KEYS 확장 금지, theme·acc·pin 은 주석 키), 출처 없는 숫자 금지(명·원은 `<Num>` 셀로만), "안전" 단언 금지, 예측 명 수 생성 금지, 새 의존성 0, 서체 서브셋 `korean` 금지(빌드 실패), `coordsOf` 는 대표점 표가 정본. 문서 숫자는 `scripts/spec-numbers.mjs`·`scripts/judge-guide.mjs` 로 생성.
