# /clear 후 붙여넣을 프롬프트

아래 블록을 그대로 복사해서 새 세션 첫 메시지로 넣으면 된다.
(2026-09-09 갱신 — `/venue` 시뮬 통합·편집기·워커가 끝난 뒤의 회차)

---

```
관광데이터 공모전 서비스 이어서 작업 (볼트 `대회/관광데이터/서비스/`).
먼저 docs/HANDOFF.md 맨 위 "2026-09-09" 절을 읽고, docs/DECISIONS.md 의 2026-09-08·09 절을 읽어.
그 아래 08-31 기록은 `/`·`/report`·데이터·작업 규약에 아직 맞다.

## 지금 상태 (한 문단)

`/venue` 는 흰 바탕 건축 도면 위 보행자 시뮬 + 담당자 배치 편집기(놓기·옮기기·돌리기 손잡이·
"쫙" 늘리기·통로 그리기·재기·저장) + 말로 묻기(모델은 질문→시나리오 번역만, 답은 시뮬 숫자).
시뮬은 웹워커에서 돈다. 엔진은 겹침(E2)·기본도표(E4, Weidmann 6/7)·워커(R2) 다 끝났고
커밋 20개가 전부 push 돼 Vercel 에 떠 있다. 유닛 227, 린트 0 오류.

## 방침 (안 바뀐 것)

- 불문율 5개(HANDOFF "절대 어기지 않는 것") 그대로. 특히 판정·시뮬에 LLM 0회 — 모델은 번역기다.
- 밀도가 7.22명/㎡ 를 넘으면 배치가 아니라 코드 버그다. 먼저 코드를 의심해.
- 엔진(lib/sim/sim.js)은 `시뮬_데모/` 에서 고치고 회귀 셋(gunpotest·overlap_test·fd_test)을
  돌린 뒤 서비스로 복사한다. 서비스에서 직접 고치지 마.
- MapLibre GeoJSON 소스 금지(Turbopack 워커 문제). 도면은 캔버스.
- 코드를 건드리면: 순수 함수 TDD → 화면 → npm test·lint·typecheck → 크롬 확인 → 커밋 → push.
  크롬 확장이 끊기면 헤드리스 크롬 CDP 로(방법은 메모리·HANDOFF).
- 화면 문구: em-dash 남용 금지, 문장당 strong 하나. `/venue` 새 문구는 아직 이 규약을 덜 지켰다.

## 이번 회차 할 일 — 하나 골라서

A. 기획안 팩트체크 화면(08 계획의 주인공). untracked `lib/verdict.ts` WIP 부터 살려서
   `/check` 3단 판정 → `/evidence`. 프리페치(`scripts/kto-prefetch.mjs --loop`) 상태부터 확인.
B. `/venue` 마무리 — 시뮬 결과를 `/report` 근거 3(도면 쏠림)에 넣기, 문구 규약 정리,
   겹친 부스 경고색(사용자는 "괜찮다"고 했으니 후순위).

내가 안 고르면 A 부터. 되묻지 말고 계획을 한 단락으로 선언하고 바로 시작해. 데모 먼저.

## 손대지 말 것

`lib/verdict.ts`·`lib/surge.ts` 수정분·`docs/redteam_2026-09-04.md` 는 지난 회차(9/5~6) WIP 다.
A 를 고르면 그때 함께 정리하고, B 를 고르면 건드리지 마.
```

---

## 이 프롬프트를 쓸 때 알아 둘 것 (붙여넣지 말 것)

- 사용자 스타일: 되묻지 않고 계획 선언 후 즉시 실행, 데모 먼저. push 도 그때그때.
- 공모전 제출물·일정은 사용자가 물을 때만 다룬다(프로젝트 CLAUDE.md).
- 크롬 확장은 세션 중 두 번 끊겼다. 끊기면 `tabs_context_mcp` 재시도, 안 되면 CDP.
