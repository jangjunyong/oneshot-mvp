# /clear 후 붙여넣을 프롬프트

아래 블록을 그대로 복사해서 새 세션 첫 메시지로 넣으면 된다.
(2026-09-09 밤 갱신 — 문구·시뮬 카드·지면까지 끝난 뒤의 회차. 코드 쪽 남은 일은 작다)

---

```
관광데이터 공모전 서비스 이어서 작업 (볼트 `대회/관광데이터/서비스/`).
먼저 docs/HANDOFF.md 맨 위 "2026-09-09 밤" 절을 읽고, docs/DECISIONS.md 의 2026-09-08·09 절(다섯)을 읽어.
그 아래 08-31 기록은 `/`·`/report`·데이터·작업 규약에 아직 맞다.

## 지금 상태 (한 문단)

제품은 세 축이다. ① `/check` 기획안 판정 → `/evidence` 실측 근거 → `/report/check` 검증 보고서 A4 2장(208+159mm) (08 계획의 주인공, 9/9 에 세움.
군포 2027 견본: 217,502 → 성립 불가, 내년 구간 평균 1.1~1.3배·최대일 1.4~1.6배. 입력은 URL, 저장 없음,
모델 0회, 명 수는 `<Num>` 출처 셀 안에만. 기획서 붙여넣기 → 확인 화면 다리 → `/check` 로 이어짐) ② `/` 진단 → `/report` 진단서 (보조 근거. 619건 배수는 재계산본이라 ①과 같은 자. 시연 = 속초시 6월 음식·미식) ③ `/venue` 시뮬.
공사 KT 프리페치는 끝났다(2019-05~2026-08 2,658일, 빠진 날 0). 619건 619/619 재계산.
유닛 257 · e2e 12 · 린트 0 오류 · typecheck·build 통과. 커밋 전부 push.

## 방침 (안 바뀐 것)

- 불문율 5개(HANDOFF "절대 어기지 않는 것") 그대로. 판정·시뮬에 LLM 0회 — 모델은 번역기·추출기다.
- 명·배·일 숫자는 `app/_components/num.tsx` 의 `<Num>` 으로만 화면에 낸다. e2e 가 출처 셀 밖 "N명" 을 센다.
- "작년" = 이력 중 가장 최근 해. 1단계 분모는 축제일 가운데 최대 전체 체류. `/check` 와 `/` 는 배수 정의가 다르다.
- 엔진(lib/sim/sim.js)은 `시뮬_데모/` 에서 고치고 회귀 셋을 돌린 뒤 복사. MapLibre GeoJSON 소스 금지.
- 코드를 건드리면: 순수 함수 TDD → 화면 → npm test·lint·typecheck → 크롬 확인 → 커밋 → push.
- 화면 문구: em-dash 남용 금지, 문장당 strong 하나. `/check`·`/evidence`·`/venue` 새 문구가 아직 덜 지켰다.

## 이번 회차 할 일 — 하나 골라서

A. `/venue` 시뮬 기본값 검토: "점 하나 = 3명"은 밀도가 부푼다(k=3 에서 7.22 초과 실측). 진단서 카드용으로 1명 기본 또는
   저장 직전 1명 재생을 권하는 안내 중 하나. 사용자 결정 뒤에.
B. 실제 기획서(PDF)로 다리 한 번 더: 배포본(키 있음)에서 붙여넣기 → 다리 → /check → 보고서까지. 단위 판독이 흔들리면 프롬프트 손질.
C. 지수 카드(`AreaTarResDemService`)는 키 등록(사용자)이 먼저. 그 전엔 손대지 않는다.

Vercel 배포본은 9/9 저녁 push 뒤 `/`·`/check`·`/report/check` 를 확인했다(아래 보고 참조).

내가 안 고르면 A 부터. 되묻지 말고 계획을 한 단락으로 선언하고 바로 시작해. 데모 먼저.

백그라운드에 dev 서버(localhost:3000)가 돌고 있을 수 있다 — `curl localhost:3000` 로 확인하고 없으면 `npm run dev`.
```
