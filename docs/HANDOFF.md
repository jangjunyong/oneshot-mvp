# 인수인계 — 2026-09-10 (맨 위가 지금. 아래로 09-09 밤·09-09 밤 시뮬·08-31 기록)

> 새 세션은 이 절을 먼저 읽고, 이어서 **`docs/verdict_2026-09-09_ralplan.md` §1.3(사슬)·§4(마디별 수용 기준 M0~M7·M12)** 를 읽는다.
> 그다음 `docs/DECISIONS.md` 의 2026-09-09·10 절. 08-31 기록은 `/`·`/report`·데이터·작업 규약에 여전히 맞다.

## 지금 무슨 회차인가 — 적대적 검증 뒤 "잔여 8일 밴드" 실행 중

2026-09-09 밤에 ralplan(Planner → Architect·Critic, 4회차, Critic APPROVE) + 독립 레드팀으로 **심사위원 관점 수상 가능성**을 검증했다.
결과 세 검토가 **67점(57~77), 상위 3 진출 13~19%** 로 수렴(주관 추정, 제출물 인용 금지). 산출물 두 개(둘 다 커밋됨):

- `docs/verdict_2026-09-09_ralplan.md` (v4) — 점수 예측·공격 12선·**실행 사슬 M0~M14**·절단 규칙 15밴드·마디별 기계 검사 기준·ADR·검토 대조
- `docs/redteam_2026-09-09.md` — 독립 레드팀(FAIL, 67점). 레드팀 CRITICAL 3("`/check` 가 서버리스에서 안 뜬다")은 curl 실측으로 기각

**사용자 결정(2026-09-09): 잔여 8일 밴드 = M0~M7 + M12 축약본**(실작업 5.6일, 예상 78점 안팎).

### 사슬 진행표 (여기를 갱신하며 간다)

| 마디 | 내용 | 일 | 상태 |
|---|---|---|---|
| M0 | 공사 유래 실측 셀 비율 측정(비픽스처 1건) | 0.2 | **끝** — `docs/measured_ratio.md`, 5/5 = 1.00 |
| M1 | 구간 즉시 정직화 — `range.ts` `confidence` 폐기, 두 화면에 "이력 범위(N년). 적중률은 아직 재지 못했다" | 0.2 | **끝** — e2e 2건 추가 |
| M2 | 문구 통일: `/`·`/venue` 첫 문단의 "예측하지 않습니다"를 "몇 배·구간" 뒤로, `sim-map` "담당자 말로는" 화자 정리, 기능설명서 "무엇을 하지 않는가" 절 제목 제거 | 0.4 | **끝** — `lib/copy.test.ts` 4건이 M2-1~4 를 기계로 잰다(유닛 263) |
| M3 | 1인당 예산 대조 카드(기획안 예산 ÷ 예상 방문객 vs ÷ 이력 실측 방문객), 새 라벨 0 → 판정 라벨 붙는 항목 3 이상 | 0.5 | **끝** — `lib/budget.ts`(+테스트 7) · `budget` URL 파라미터(만 원) · `/check` 카드·행·폼, `/report/check` 행 · e2e 13번째. 견본 예산 10억은 **가정값**(공개 수치 없음, 셀 출처에 명시) |
| M4 | 로고 통일(`.logo` 유일값 1) · 나침반 순서 `/check → /evidence → /venue`, `/` 는 "보조" · `/report` 상단에 `/report/check` 링크 | 0.3 | **끝** — 로고 "기획안 팩트체크"(여섯 화면, 보고서 힌트에도) · 나침반 `/check → /evidence → /venue → /(진단(보조))` · `lib/copy.test.ts` M4-1~3 |
| V1 | (사용자 요청, 09-10 낮) 엔드포인트별 클릭 검증 + critic 3인 적대적 검증 → 확정 결함 21건 수정. `docs/critic_endpoints_2026-09-10.md` | 0.4 | **끝** — derived 출처, 연인원 단서, 0.2371, 표제란·저장소 문구, UUID id, 서체 사슬, `/venue` 표기 4건, metadata 5장 |
| M5 | −52주 근사 백테스트 `scripts/range-backtest.mjs` → `docs/range_backtest.md` + 상수·유닛·e2e. 문구 3요소(근사·표본 N·편향 양방향) 필수, n=2 < 30건이면 백분율 포기 | 0.8 | **끝** — `lib/backtest.ts`(순수, 0.8초) · 표본 381(n=2 325 · n=1 56) · 최대일 구간 덮음 n=2 **51.4%** · n=1 55.4% · 통과군 배수 1.79 vs 탈락군 1.25(쏠림 실측) · 군포 근사=실측(차이 0) · 두 화면 문장은 `backtestSentence` 하나 · e2e 14번째. M1 문장은 "적중률은 −52주 근사로만 쟀다"로 |
| M6 | `counting` 을 계산에 반영 + 손익분기 회전율(가정 상수 0) · 라벨 "성립 불가" → "상한 초과" (`grep -rn "성립 불가" lib/ app/ e2e.test.mjs` 0건, `app/layout.tsx:28` 포함) · e2e :382·:386·:431 갱신 · 완료조건 #13 회귀 갱신, 음성 "통과" 유지 못 하면 되돌림 | 1.0 | |
| M7 | `/check` 견본 2건(지정축제 1 + 군포), 둘 다 "근거 없음" 아님, 이력 기간 픽스처에 출처. **입력(09-10 critic)**: 군포 견본을 60만(기간 총계, 경인일보 2026-04-17)로 바꾸고 예산은 빈칸으로 두는 안 — M3-5(라벨 3개)와 충돌하므로 지정축제 견본에서 공개 예산으로 채운다. `critic_endpoints` §3b | 0.7 | |
| M12 축약본 | 기능설명서 §1~§3 교체 + §4 절 제목을 해시태그 4개로 + 숫자 생성 스크립트(정밀도·재현율·리프트·일수·시군구·유닛·e2e 문자열 일치 유닛) | 1.5 | |

공통 회귀(모든 마디 뒤): `npm run lint && npm run typecheck && npm test && npm run build && npm run test:e2e`. 마디마다 커밋 하나, push.
R7 기준선: `Label` 유니온은 M6 전엔 "성립 불가" 포함 7종, 후엔 "상한 초과" 포함 7종. M3·M11 은 원소를 추가하지 않는다.

### 사용자만 할 수 있는 것 (검증이 지목, 아직 미해결)

1. **사무국(070-4060-4046) 1차 심사자료 제출일 확인.** 무회신이면 9/15부터 잔여 5일 밴드(M0~M4 + M12 축약본)로 자동 축소.
2. **`AreaTarResDemService` 활용신청**(data.go.kr). 승인 대기가 있어 늦으면 못 붙인다.

### 검증 상태 (2026-09-10 아침)

- 유닛 **286** · e2e **14** · 린트 0 오류 · typecheck·build 통과 (09-10 M5 뒤)
- 커밋 전부 push, Vercel 배포됨. `/check`·`/evidence`·`/report/check` 배포본 200 실측(9/9)

### 반드시 알아야 할 것 (이번 회차)

- **점수·확률 숫자는 제출물에 인용하지 않는다.** 검증 문서 안에서 마디 우선순위 비교용이다.
- 사슬은 **뒤에서 자른다.** 잔여 달력일 × 0.7 이 예산. 밴드 표는 verdict §1.3.
- 백테스트(M5)는 "같은 축제 다년 표본"이 0건이라(619건은 축제당 1행, TourAPI 는 과거 연도 미보존) −52주 근사로만 가능하다. 근사임·표본 수·편향 양방향을 화면에 다 적어야 한다. `HANDOFF` 의 "이력 자동 탐지 금지"는 제품 입력 규칙이지 방법 검증 규칙이 아니다.
- M6 의 회전율은 **가정하지 않는다**. 손익분기 회전율(N ÷ (0.80 × 최대 전체 체류) = 군포 1.04)만 낸다.
- 새 의존성 0, 새 화면 0, `lib/surge.ts` 불변, 판정·시뮬에 LLM 0회.
- **구간 적중률은 −52주 근사값이다(M5).** n=2 표본 325건에서 최대일 구간이 올해를 덮은 비율 51.4%. 절반이다. 화면은 이 숫자를 "대형 축제 표본에서의" 한정어와 편향 양방향 문장 없이 내지 않는다. `docs/range_backtest.md`.
- **`Measured.origin` 에 `derived` 가 생겼다(09-10 V1).** 담당자 입력 ÷ 공사 실측처럼 두 출처가 섞인 값은 measured 로 찍지 않는다(출처 세탁). verdict M3-1 의 "measured + KT_API" 요구는 이 이유로 따르지 않았다. `<Num>` 은 점선 밑줄, 툴팁 "(입력 ÷ 실측)".
- **디자인 변경은 계획안만 있다** (`docs/design_plan_2026-09-10.md`, 접근 A/B/C, 추천 A). 사용자 결정 전에는 구현하지 않는다. taste-skill 4종은 `~/.claude/skills/` 에 설치됨.

---

# (2026-09-09 밤 기록) 인수인계 — `/venue` 지도 아래는 09-09 밤·08-31 기록

> 새 세션은 이 절을 먼저 읽고, 이어서 **`docs/DECISIONS.md` 의 2026-09-08·09 절(다섯)**을 읽는다.
> 08-31 기록(아래)은 `/`·`/report`·데이터·TourAPI·작업 규약에 여전히 맞다.

## 지금 제품은 세 축이다

1. **기획안 팩트체크** (`/check` 판정 → `/evidence` 실측 근거 → `/report/check` 검증 보고서 A4 2장) — **08 계획의 주인공, 2026-09-09 낮·오후에 세웠다.** 담당자가 쓴 예상 방문객을 공사 KT 실측과 같은 자로 재서 통과/주의/과대/과소/성립 불가/근거 없음/단위 미상을 찍고, 내년 배수 구간(자기 이력 + 또래)을 낸다. 견본은 군포 2027(이력 2024·2025·2026). 커밋 3개, push 됨.
2. **축제 위험 경보** (`/` 진단 → `/report` 진단서) — 이제 **보조 근거**. 619건 배수는 09-09 저녁부터 재계산본(`lib/surge.ts` 정의)이라 `/check` 와 같은 자다. 첫 화면은 기획안 판정이 앞이고, 기획서 붙여넣기 → 확인 화면의 다리 → `/check` 로 이어진다. 시연 사례는 속초시 6월 음식·미식.
3. **행사장 시뮬레이션** (`/venue`) — 09-08~09 통합. 아래 "`/venue` 파일 지도" 절.

## `/check`·`/evidence` 파일 지도 (2026-09-09 낮)

| 파일 | 역할 |
|---|---|
| `lib/verdict.ts` (+ `.test.ts` 9건) | 판정 엔진. 3단 검사(상한 하드 게이트 / 순증 보조 신호 / 동일 정의 배수 주 근거) + 최종 집계 + 요일·기간 항목 + 설명 문장(숫자는 슬롯). `Verdict` 에 명·원 스칼라 0개 |
| `lib/history.ts` | `computeSurge` 결과 → `HistoryYear[]`(연도 오름차순, 자료 밖은 이유와 함께 skipped) |
| `lib/range.ts` | 내년 구간 — 연도별 배수 min~max 를 0.1 단위 바깥 반올림. 또래 구간은 중앙값~상위 5% |
| `lib/peerband.ts` | 재계산 619건(`data/festivals.surge.json`)에서 같은 인구 구간의 또래 배수 |
| `lib/checkquery.ts` | URL ↔ 입력. 아무것도 없으면 군포 2027 견본(`GUNPO_2027`). 이력 슬롯 3개 |
| `app/_components/num.tsx` | `<Num>` — 명·배·일은 이것으로만. `data-num` + 출처 4속성 |
| `app/check/page.tsx` | GET 폼 + 판정표 + 다른 항목 + 단서 + 내년 구간 카드 + 자기 이력 표 |
| `app/evidence/page.tsx` | 연도별 전후 4주 외지인 곡선(SVG·같은 축척) + 배수·순증 표 + 최근 회차 일별 표 |
| `app/report/check/page.tsx` | 검증 보고서 A4 2장. 1장 결론·문장·판정표·내년 구간·보완·귀속 경고 / 2장 연도별 실측·판정 셀 출처·정의·임계·단서. 지면 218+159mm |
| `lib/extract.ts` `sanitizeFacts` · `lib/checkquery.ts` `checkUrlFromExtraction` | 기획서 → 팩트 8필드(근거 없으면 null) → `/check` 링크. `/` 확인 화면의 다리 절 |
| `lib/overlap.ts` `attributionCaveat` | 작년 기간 반경 50km 공사 TourAPI 등록 축제 → 귀속 경고 한 문장. 세 화면이 같이 쓴다 |
| `lib/festivals.ts` | 619건 `actualVisitSurge` 를 재계산본(`festivals.surge.json`)으로 덮는다. `RECOMPUTED` 건수 |
| `lib/simcard.ts` | `/venue` 저장 시점의 시뮬 요약(`Venue.sim`). 옛 진단서 `/report` 근거 3 이 읽는다. k>1 이면 밀도 부풂 경고 |
| `data/kto/visitors/*.json.gz` | 시군구 299개 × 2,658일. `scripts/kto-build.mjs --fixture 41410` 산출. **프리페치는 끝났다** |
| `e2e.test.mjs` 끝 3건 | 견본 성립 불가·구간, 음성 통과·단위 미상·첫 회 또래, 곡선 3장, 출처 셀 밖 "N명" 0건 |

## 검증 상태 (2026-09-09 낮)

- 유닛 **259** · e2e **12** · 린트 0 오류(경고 1, 프리페치 스크립트) · `npm run typecheck` **통과** · `next build` 통과
- 옛 진단서 지면(재계산본 + 시뮬 카드): 189mm(카드 있음) · 견본 245mm. 가용 273mm
- 자기검증(LOO, 재계산본): 정밀도 65.8% · 재현율 61.6% · 리프트 3.26 · ±0.3배 안 80.9%. 시연 사례 속초시 6월 음식·미식 = 심각·감당 범위 상한 1.83·기준 실향민문화축제
- 회귀(08 §4 #13): 217,502 → 성립 불가(0.83 / 7.71 주의 신호 / r 1.96 과대) · 110,184 → 통과(0.42 / 3.90 / 0.99) · 단위 없음 → 단위 미상
- 내년 구간(#1): 평균 1.1~1.3배 · 최대일 1.4~1.6배(3개년) — 2개년이면 계획서 값 그대로
- 크롬 실측: 판정표·구간 카드·자기 이력 표·곡선 3장·일별 표. Vercel 은 push 직후 자동 배포(확인은 아래 "남은 것")

## 반드시 알아야 할 것 (팩트체크 쪽)

- **"작년" = 이력 중 가장 최근 해.** 1·2단계 분모가 그 해에서 나온다. 계획서 숫자(0.92·7.6)는 2025 기준이고 지금은 2026 기준 — 라벨은 같다. `lib/verdict.test.ts` 머리 주석.
- **1단계 분모는 축제일 가운데 최대 전체 체류**(현지인+외지인+외국인). 발표 최다일이 아니다.
- `/check` 와 `/` 는 이제 **같은 배수 정의**다(09-09 저녁, `lib/festivals.ts` 덮어쓰기). 08 계획의 "grade/eval/capacity/season 폐기"는 안 했다 — 값만 바꿨다. `data/festivals.json` 의 원본 배수는 대조용.
- 명 수를 화면에 낼 때는 `<Num>` 을 쓴다. 문장 조각은 `explainVisitors` 처럼 슬롯 뒤 조사로 시작한다(`<Num>` 이 단위까지 찍는다).
- 이력 기간은 담당자 입력. 자동 탐지 없음(짐작 금지).

## 남은 것 · 다음 후보

- **Vercel 배포본 확인** — `oneshot-mvp.vercel.app/check` 가 견본을 띄우는지(자료 8.4MB 가 서버 파일로 같이 올라가야 한다. `data/kto/visitors` 는 git 에 있으니 되겠지만 눈으로 확인)
- ~~`/report` 에 판정 보고서(08 #12, A4 2장)~~ — `/report/check` 로 끝났다(오후). 옛 `/report` 는 그대로다
- ~~LLM 추출 9축 개정(08 #6b)·다리~~ — 끝났다(저녁). 키가 있는 배포본에서 실제 기획서로 한 번 더 확인할 것(단위 판독이 흔들릴 수 있다)
- ~~반경 50km 경쟁 경고(`overlap.ts` 재연결, 08 #11a)~~ — 끝났다(저녁). 지수 카드(`AreaTarResDemService` 키 등록)는 사용자 액션
- ~~옛 `/report` 지면 재기~~ — 쟀다(밤): 189·245mm
- ~~`/venue` 시뮬 결과를 `/report` 근거 3 에~~ — 끝났다(밤, `lib/simcard.ts`). 시뮬을 돌린 뒤 "도면 저장"을 눌러야 카드가 남는다
- ~~문구 규약~~ — 산문 em-dash 걷음(밤). 절 제목의 "—" 는 규약
- ~~`docs/기능설명서.md` 숫자~~ — 고쳤다(밤 2). 구조는 08-31 제품 기준이라 다시 써야 한다(머리에 적음)
- ~~`/venue` 점 하나 기본값~~ — 1명(밤 2)
- 추출기: 읍면동만 적힌 기획서는 시군구를 못 붙인다. `/check` 에서 담당자가 적는다. 실호출 기록은 DECISIONS "밤 2"
- 화면 문구 규약(em-dash 남용 금지, 문장당 strong 하나) — `/check`·`/evidence` 새 문구도 아직 덜 지켰다. `/venue` 와 함께 한 번 훑을 것
- `/venue` 마무리(B): 시뮬 결과를 `/report` 근거 3 에, 겹친 부스 경고색(후순위)
- 09-09 밤 항목(아래 절) 그대로: 편집기 도로 다각형, 3D 라벨 겹침, 질문 한도 카운터

---

# (2026-09-09 밤 기록) `/venue` 시뮬 통합

## `/venue` 파일 지도 (2026-09-09)

| 파일 | 역할 |
|---|---|
| `app/venue/sim-map.tsx` (~1,300줄) | 화면 전부. MapLibre 배경 + 캔버스에 도면·사람·밀도·편집 상태를 직접 그린다. 편집 모드 5개(보기/선택·옮기기/부스 놓기/통로 그리기/재기), 손잡이(회전·늘리기), 질문 패널, 스트레스, KPI |
| `app/venue/sim.worker.ts` · `sim-protocol.ts` | 격자·거리장·스텝·질문(headless)·스트레스가 전부 워커. 50ms 마다 프레임(좌표 Float32Array 전송, 5프레임마다 밀도) |
| `lib/sim/sim.js` · `geo.js` (+ `.d.ts`) | 엔진. **`시뮬_데모/sim.js` 와 같은 파일** — 데모에서 고치고 검증한 뒤 복사한다 |
| `lib/geoedit.ts` | 편집 순수 함수 — 놓기·옮기기·돌리기·지우기·통로·맞히기·이웃 줄/통로 스냅·절대각·늘리기(extendRow). 테스트 16건 |
| `lib/simask.ts` · `app/venue/ask-action.ts` | 담당자 말 → 시나리오 JSON(모델은 번역만). 번호 지목("17번")은 규칙이 모델을 이긴다. 키 없음·실패·하루 60건 초과면 규칙 폴백 |
| `public/venue/gunpo.geojson` | 군포 도면(OSM 부지·보행로 + 안내도 부스 114). `시뮬_데모/build_gunpo.py` 가 만든다 |
| `app/venue/page.tsx` | 서버: 저장·불러오기(`Venue.geo`), 쌍둥이 배수 → 시뮬 배수 기본값. 예전 Konva 편집기는 `<details>` 아래 접혀 있다 |
| `app/twin-map-3d*.tsx` | `/` 의 3D 쌍둥이 지도(보조·클릭해야 뜸). 무JS SVG 지도는 그대로 |
| `시뮬_데모/` | 헤드리스 시험대. **회귀 셋**: `node gunpotest.mjs && node overlap_test.mjs && node fd_test.mjs` |

## 검증 상태 (2026-09-09 밤)

- 유닛 **227** 통과 · 린트 0 오류 · `npx tsc --noEmit` 는 untracked `lib/verdict.ts` 만 빨갛다(아래)
- 엔진: overlap 0쌍·최대 밀도 ≤ 7.22 / Weidmann 기본도표 7점 중 6점 ±30%(전부 −20~−30%, 느린 쪽) / 60분 전 부하 최대 6.00 / 도달성 123/123
- 크롬 실측: 워커 격자 5초(배포본 15초), 재생·질문(1분 안팎)·편집 클릭 왕복 48ms, 회전 손잡이 +20°, 늘리기 4개
- Vercel: `oneshot-mvp.vercel.app/venue` 배포·동작 확인

## 반드시 알아야 할 함정 (시뮬 쪽)

- **MapLibre 6 GeoJSON 소스는 쓰지 마라.** Next(Turbopack) 아래서 워커가 응답하지 않아 `loaded` 가 영영 false 다(오류 이벤트 0). 도면은 캔버스에 그린다. 우리 워커(`sim.worker.ts`, `new Worker(new URL(...), import.meta.url)`)는 된다.
- 배경을 갈거나 첫 로드 뒤 **타일이 안 뜨면 `triggerRepaint`**. 코드에 반 초마다 넣어 뒀다.
- 도면 좌표는 OSM 과 오차 0. 어긋나 보이면 **도로 모델**(폭 26m·중심선 부스 쪽 2m, ±3m)이다. 위성 배경에서는 도면을 가장자리 선으로 그려 연석과 대조한다.
- 밀도 상한 7.22명/㎡ 를 넘으면 **버그다**(반지름 0.2m 최밀 충전). 지금까지 두 번 넘었고 둘 다 코드였다(겹침 거부 규칙 / 부지 밖 줄 자리의 nearestWalkable 겹침).
- 편집 모드에서는 격자를 다시 만들지 않는다(보기로 돌아올 때). 손잡이 히트는 캔버스 좌표(×dpr)로 메인이 잡는다 — 합성 마우스로 시험할 때 프레임 하나(60~100ms) 기다려야 손잡이가 있다.
- 크롬 확장이 끊기면 **헤드리스 크롬 + CDP**(Node 내장 WebSocket)로 검증할 수 있다. 패턴은 메모리에.
- `personsPerAgent(k) > 1` 이면 줄·서비스 자리 기하가 k 를 안 따라가 밀도가 부풀 수 있다. 질문 재생은 k=5·60분(1분 안팎). k=1 이면 80초/60분.

## 남은 것 · 다음 후보

- **기획안 팩트체크 화면** — 08 계획의 주인공. `/check` 3단 판정·`/evidence`. `lib/verdict.ts`(WIP, HistoryYear 에 `periodTotal`·`periodTotalOrNull` 없음) 부터 살려야 한다. 프리페치는 하루 1,000일씩, 9/8 기준 667일 남음(`node scripts/kto-prefetch.mjs --loop`)
- 편집기: 늘려 놓은 부스가 기존 부스와 겹치면 경고색(사용자 "괜찮을 것 같음" — 보류), 도로 다각형을 위성에서 직접 따기, 부스 크기(3×3 외) 선택
- 3D 지도: 같은 자리 기둥은 옆으로 비켜 서지만 라벨은 겹칠 수 있다
- 시뮬 결과가 `/report` 근거 3(도면 쏠림)에 안 들어간다 — 아직 예전 Konva 도면만 본다
- 화면 문구 규약("em-dash 남용 금지, 문장당 strong 하나") — `/venue` 새 문구가 이 규약을 다 지키지 못했다. 답 카드·인스펙터 문구를 한 번 훑을 것
- 질문 하루 한도는 프로세스 메모리 카운터(서버리스면 인스턴스별). 진짜 상한은 오픈라우터 키별 지출 상한

## 손대지 않은 것 (지난 회차 9/5~6 WIP, 커밋 안 됨) — **09-09 낮에 해소.** `verdict.ts`·`surge.ts` 는 판정 엔진 커밋에, 레드팀 문서는 docs 커밋에 들어갔다

`lib/verdict.ts`(untracked) · `lib/surge.ts` 수정분(+14줄, `frn/total/periodTotal` 필드) · `docs/redteam_2026-09-04.md`(untracked). `verdict.ts` 가 로컬 `next build` 를 막지만 untracked 라 Vercel 엔 영향 없다. **이 셋은 이번 회차 코드가 아니다** — 08 계획 이어갈 때 함께 정리.

---

# (2026-08-31 기록) 인수인계

> 새 세션은 이 파일부터 읽고, 이어서 **`docs/DECISIONS.md`** 를 읽는다.
> 이 파일은 "지금 무엇이 있나", DECISIONS 는 **"무엇을 안 하기로 했고 왜인가"** 다 — 버린 길을 두 번 파지 않으려고 나눠 뒀다.
> 코드 규약은 `CLAUDE.md`, 화면 구조는 `docs/wireframe.md`·`docs/screens.md`.

## 이 제품이 무엇인가

축제 기획안을 넣으면 **닮은 과거 축제 619건의 실측 방문 배수**로 위험 등급을 내고,
그 쏠림을 **행사장 도면 위에서 미리 잡아 보는** 웹서비스.

목표는 하나다 — 지자체 담당자가 **"왜 물량을 3배로 잡았습니까"에 댈 근거**를 갖는 것.
그래서 정확도가 아니라 **출처**가 값어치다.

- 배포 https://oneshot-mvp.vercel.app · 저장소 https://github.com/jangjunyong/oneshot-mvp
- 마지막 **코드** 변경 = `c3ca15e` (배포됨). 그 뒤는 문서 커밋이다.
  지금 값은 `git log --oneline -5`. 백업 브랜치 `backup/2026-08-30-야간작업`.
- 검증: 유닛 **194** + 무JS e2e **8** (`npm test` · `npm run test:e2e`)

## 지금 겨누는 것

**2026 관광데이터 활용 공모전 ②-2 웹·앱 구현 부문, 지정과제 9번**
1차 심사 배점: 구현성 30 · 기획력 30 · **데이터 활용(공사 OpenAPI 필수) 20** · 발전성 20

**일정 (공고문 원문 확인, 2026-08-31)**
```
공고 및 접수   7.6 ~ 7.21 16:00   <- 이미 끝났다
예비심사·OT    7월 말
서비스 개발    7월 말 ~ 9월        <- 지금 여기, 마지막 달
1차 심사       10월 중  (서면 + 기능심사)
최종 심사      10월 중  (상위 3팀 PT)
시상식         11월 중
```
> **1차 심사자료 제출일은 공고문에 없다.** 개별 안내로 오는 값이다.
> 메모에 "9/21" 이 있는데 출처가 확인 안 됐다 — **사용자가 확인해야 한다.**

- 수업 과제는 끝났다. 공모전만 본다.
- **방침(2026-08-30): 기능이 조잡해도 생각한 대로 완성.** 새 아이디어를 벌이지 않는다.
- **서비스는 끝났다(2026-08-31).** 그래서 "문서·제출물은 하지 않는다"던 임시 방침의
  근거가 사라졌다. 지금 남은 일은 **제출물**이다.
- 모바일은 여전히 **범위 밖**(사용자 배제).

### 1차 심사가 실제로 요구하는 것

배점: 구현성 30 · 기획력 30 · **데이터 활용(공사 OpenAPI 필수) 20** · 발전성 20

지정과제 **9번** — *"축제 수요 예측 실패 및 주관적 경험 의존형 기획으로 인한
예산 낭비 리스크 존재 및 대규모 관광객 쏠림에 따른 축제 만족도 저하"*
타깃도 그대로 맞는다: *"축제 예산 집행 전 기획안의 객관적인 검증이 필요한
지자체 및 축제 위탁사 실무자"*

**지정과제 목록 붙임이 예고한 것 (놓치면 안 된다)**
> 향후 1차 심사자료 제출 시, **해시태그와 연관된 기능 구현 내용에 대해
> 기능설명서 작성을 요청할 예정**

과제 9번 해시태그: `#축제/행사 기획` `#사전 수요 예측` `#보완 피드백 제공` `#데이터 기반`

**두 번째 해시태그가 우리 불문율 1번과 정면으로 부딪힌다.** 과제 예시는
"사전 흥행 가능성을 **예측**·진단"이라 쓰는데 우리는 "예측하지 않는다"가 제품의
첫 규칙이다. 숨기면 과제 미부합으로 읽혀 **심사 제외 사유**가 되고, 정면으로
다루면 **기획력 30(타당성·독창성)의 최고 재료**가 된다. 논거는 이미 있다 —
사용자 통찰("기획안은 25일 전, 사람들은 3일 전에 정한다")과 619건 실측.

## 절대 어기지 않는 것 (제품 불문율)

1. **방문객 수를 예측하지 않는다.** 화면 숫자는 과거가 실제로 겪은 *평소 대비 배수*뿐이다.
2. **"안전하다"고 말하지 않는다.** 근거를 못 찾은 것과 안전한 것은 다르다.
3. **왜 닮았는지를 항상 같이 낸다.** 유사도 점수만 던지면 실무자는 못 믿는다.
4. **출처 없는 숫자·그림을 화면에 올리지 않는다.** 못 구한 값은 비우고 "못 구했다"고 적는다.
5. **판정·시뮬에 LLM 0회.** 모델은 기획서에서 항목을 옮겨 적는 1회만.

`evals/cases.md` 09행이 4번의 판정 기준이다. **2026-08-31 에 판정 문장을 고쳤다** —
옛 문장("화면의 숫자가 전부 입력값·지역 인구·실측 배수·연도 중 하나인가")은 화면을 못
덮어 **한 번도 통과한 적이 없었다**(부하 지수·부지 면적·적중률·또래 순위가 다 그 네
범주 밖이고, 어느 것도 방문객 수 예측이 아니다). 지금 판정은 둘이다.

1. 화면의 숫자가 입력·실측·실측에서 센 값·도면에서 잰 값으로 **되짚어지는가**
2. **사람 수(명)와 금액(원)이 붙은 숫자가 0건인가** ← `e2e.test.mjs` 가 매번 잰다

기대는 안 바뀌었다("방문객 수 예측값을 출력하지 않는다"). 자의 눈금만 고쳤다.

## 화면 셋 (현재 동작 상태)

### `/` 진단 — 다 붙어 있고 돈다
입력 3경로(기획서 붙여넣기·PDF / TourAPI 축제 검색 / 직접 입력) → 5축 확정 → 결과.
결과 블록 순서는 **결론 → 근거 → 신뢰도**다.
```
왼쪽 열                        오른쪽 열
─────────────────────────      ──────────────────────────────
지도 (닮은 축제 핀 1·2·3)      경보 등급 + 한 문장
카드 넷 (2×2)                  배수의 뜻 + 또래 비교
  ① ② ③ 닮은 축제               (인구 편향을 여기서 밝힌다)
  넷째 = 어떻게 골랐나         감당 범위        (심각·주의일 때만)
  (기준 없으면 그 사실)        왜 닮았나        (details)
또래 분포 띠                   같은 시기 경쟁   (키 없으면 섹션째 숨김)
                               시기 민감도      (12행 표는 details)
                               자기검증         (details)
```
**닮은 축제 목록은 오른쪽에 없다.** 지도의 핀을 설명하는 것이라 지도 옆 카드로
옮겼다(2026-08-31). 카드를 누르면 오른쪽에 그 축제의 핀 카드가 펴진다.

### `/venue` 행사장 도면 — 돈다
"부지 지도 깔기" 한 번에 **200×140m 예시 16개**가 같이 깔리고 부지 28,000㎡·쏠림 스캔이 즉시 나온다.
브이월드 백지도만 쓴다(위성 토글은 뺐다). 부지에 맞춰 보기(타일 오버줌) 있음.

### `/report` 진단서 — 돈다
근거 1(닮은 축제)·2(같은 시기 경쟁)·3(**도면 쏠림 — 도면을 저장해야 채워진다**)·4(시기 민감도)·5(감당 범위) + 자기검증 + 한계 고지.

**A4 한 장은 실측으로 지킨다.** A4 가용 높이는 273mm(297 − 위아래 12mm).
```
실제 진단서 최악  260mm (여유 13)   ← 결재에 쓰는 문서
시연 견본 기본    244mm (여유 29)
시연 견본 최악    257mm (여유 16)
```
자동 검사가 없다. 진단서에 뭘 더 넣으면 **인쇄 규칙만 켜고 다시 재라**
(아래 "작업 규약"에 방법). 제일 먼저 넘치는 곳은 `REPORT_RIVALS`(지금 3) 다 —
경쟁 목록만 길이에 상한이 없다.

---

## 여기까지 온 길 — 무엇이 끝났나

> 새 기능을 벌이지 말 것. 아래는 전부 **이미 있는 것을 끝까지 이은** 일이다.
> 취소선은 끝난 것이고, 근거는 전부 `docs/DECISIONS.md` 에 있다.

### ~~1. 시연 세트를 코드로 고정~~ — 끝났다 (`lib/demo.ts`)
이력이 0건이면 예시 기획안 진단 1건 + 예시 도면 1건이 대신 선다.
**강원 횡성군·6월·음악·공연**을 새로 연다고 가정한 것이다. 지역·인구·접근성은 619건의
횡성군 값이고, 진단·진단서·도면 세 화면에 "시연용 예시"라고 적는다.
**619건 어느 축제와도 조건이 같지 않다** — 처음엔 고령 대가야축제 등록값을 그대로
썼다가 자기 자신을 쌍둥이로 집는 것이 적대적 검증에서 드러나 옮겼다.
이력 건수에는 안 넣는다 — 화면은 여전히 "0건"이라고 말한다.
`lib/demo.test.ts` 가 심각 등급·감당 범위·도면 쏠림 셋이 계속 뜨는지 매번 다시 잰다.

### ~~2. 빈 상태 점검~~ · ~~3. 진단서 A4~~ — 끝났다
넷 다 눈으로 봤다. 쌍둥이 0곳에서 시기 표가 `(폭 배)`라고 말하던 버그를 잡았다.
진단서는 329mm(두 장)였다 — 인쇄 밀도와 시기 표·경쟁 목록을 줄였다.
자세한 근거는 `docs/DECISIONS.md` 의 "완성 회차 1".

### ~~4. 지도 아래 빈 땅~~ — 카드 넷으로 메웠다 (2026-08-31)
왼쪽 열이 오른쪽보다 470px 짧았다(사용자 지적). 닮은 축제 목록을 지도 옆으로
옮겨 2×2 로 깔았더니 29px 로 줄었다. 넷째 칸은 "어떻게 골랐나"이고,
감당 범위 기준을 못 찾았을 때는 그 사실이 그 자리를 맡는다.

### ~~자동 추출 모델~~ — 유료로 바꿨다 (2026-08-31, 결정이 한 번 뒤집혔다)
8/30 에는 "그대로 둔다"였다. **1차 심사가 심사위원이 직접 버튼을 누르는
기능심사**라는 것을 확인하고 뒤집었다 — 첫 관문이 429 면 구동 안정성이 그 자리에서
깎인다. 후보를 추측이 아니라 같은 김천 기획서로 직접 돌려 골랐다.

```
google/gemini-2.5-flash-lite   1.0초  5축 정확      약 1.1원/건  <- 채택
upstage/solar-pro4            15.0초  5축 정확      약 0.3원/건
openai/gpt-oss-120b           16.2초  대부분 못 뽑음
openai/gpt-5-nano                 —   우리 스키마를 거절(400)
```
값보다 **1초**가 컸다. 심사위원은 15초를 안 기다린다.

**비용은 셋으로 잠겨 있다** — 입력 8,000자(`MAX_PLAN_TEXT`) · 응답 700토큰
(`MAX_OUTPUT_TOKENS`) · 하루 45건(`DAILY_EXTRACT_LIMIT`). 곱해도 하루 50원 안쪽.
모델 id 는 **코드 기본값**이다. Vercel 의 `OPENROUTER_MODEL` 은 지웠다 — 환경변수로
숨겨 두면 어느 모델이 도는지 아무도 모른다.

> **아직 안 한 것: 오픈라우터 대시보드의 키별 지출 상한.** 배포본은 로그인이
> 없어 주소를 아는 사람은 누구나 누른다. **사용자만 걸 수 있다.**

### ~~검색량 축~~ — 재고 버렸다 (2026-08-31)
키를 받아 실제로 쟀다. 표본 크기를 넷으로 바꿔 가며 재니 ρ 가 0.243 · **0.340** ·
0.221 · 0.236 이었다. 컷(0.3)을 넘는 것은 넷 중 하나뿐이고 가장 큰 표본(n=237)이
0.236 이다. 피어슨은 0.084~0.515 로 널뛴다 — 몇몇 이상치가 끄는 것이다.
**표본을 바꾸면 뒤집히는 신호는 신호가 아니다**(`season.ts` 의 `robust` 와 같은 논리).
파일은 남긴다. 다시 "검색량 쓰면 되잖아"라는 물음이 오면 답이 거기 있다.

### ~~적대적 검증 회차~~ — 끝났다 (2026-08-31)
비평가 셋(판정 논리·코드 회귀·제품 불문율)을 붙이고, 지적을 그대로 믿지 않고
코드와 619건으로 **직접 재서 확인된 것만** 고쳤다. 자세한 것은 DECISIONS.
셋이 같은 병을 짚었다 — **재지 못한 것이 늘 "안심" 쪽으로 기운다.**

### ~~구조 적대적 검증 회차~~ — 끝났다 (2026-08-31 밤)
"폴더구조가 스파게티"라는 의심으로 크리틱 셋 + 레드팀 하나를 붙였다.
**전제가 틀렸다** — `lib/` 21개 flat 은 군집이 이미 넷으로 갈려 있고
지오메트리에서 점수계산으로 나가는 간선이 0개다. 폴더링은 실익이 없다.
대신 구조가 아닌 것 셋을 고쳤다. 자세한 것은 DECISIONS 맨 끝.

- **도면 화면이 진단서와 다른 배수를 쓰고 있었다** — `venue/page.tsx` 가
  중앙값을 직접 다시 셌는데 `grade.ts` 와 방향이 반대(아래쪽 vs 위쪽)였다.
  짝수 매치 **6.0%**(51,264 입력 중 3,071)에서 갈린다. 화면은 "넘는 부스 없음",
  종이는 "넘칩니다"가 되는 조합이 있었다. `grade(r).medianSurge` 로 통일.
- **`npm test` 의 glob 이 비재귀였다** — 테스트를 하위 폴더로 옮기면 **exit 0 으로
  조용히 사라진다**(Node 24.19 로 직접 확인). `"lib/**/*.test.ts"` 로 바꿨다.
  따옴표가 핵심이다 — 없으면 CI 의 bash 가 globstar 꺼진 채 `*` 로 취급한다.
- **CI 가 테스트를 하나도 안 돌리고 있었다** — 타입·린트·빌드뿐이었다.
  `npm test` 와 `npm run test:e2e` 를 넣었다. 불문율 1번을 재는 유일한 자가
  머지 게이트 밖에 있었다.
- **`Entry → PlanInput` 변환을 `lib/types.ts` 의 `planInputOf` 한 곳으로 모았다** —
  위 중앙값 버그의 근인이다. 화면 여섯 곳이 손으로 복제하고 있었고, 이름이
  바뀌는 축(`theme` → `themeCode`)이 있어 잘못 이어도 타입이 안 잡는다.
  덤으로 `Entry` 를 `store.ts` 에서 옮기면서 **`store ↔ demo` 순환이 끊겼다**
  (lib 전체에 하나뿐이던 순환). `lib/types.test.ts` 가 배선을 잠근다. 171 → 175.
- **도면 편집기가 렌더마다 밑그림 4MB 를 다시 직렬화했다** — 밑그림을 폼에서
  분리하고 서버가 합친다. 렌더당 **4.77MB → 0.06KB**(실측). 밑그림을 지우는
  경로가 없으므로 빈 값은 "안 올렸다"는 뜻이고 덮어쓰지 않는다.
- **`monthWindow` 가 달이 아닌 값에 창을 지어냈다** — `Number("")=0` 이
  전년 12월 말일이 되어 엉뚱한 달의 경쟁 축제가 근거인 척 뜬다. `null` 로 막았다.
- **등급 문구가 화면·진단서에 각각 복제돼 있었다** — 중앙값과 같은 실패 유형이라
  `levelLabel` 로 모았다. 이력 칩용 짧은 변형은 일부러 다르다. 175 → 179.

- **`app/page.tsx` 를 분해했다 (1,398 → 902줄)** — 서버 액션 4개를
  `app/actions.ts` 로, 표시 전용 컴포넌트 넷을 `app/_components/` 로,
  한국 시각 포매터 세 벌을 `lib/datetime.ts` 한 곳으로. **렌더된 HTML 12화면이
  바이트 단위로 동일**한 것을 확인하고 옮겼다(`evals/golden.mjs`).
  액션을 옮긴 이유는 줄 수가 아니다 — 컴포넌트 안에 있으면 요청 파라미터를
  한 줄 끌어다 쓰기 쉽고, 그러면 폼을 낸 시점이 아니라 화면을 그린 시점의
  값이 클로저에 얼어붙는다. 파일을 가르면 그 변수들이 애초에 안 보인다.
- **`store.ts` 의 `ready()` 3중 복제를 없앴다** — 캐싱 규칙을 `onceOrRetry`
  라는 순수 함수로 빼고 세 `ready` 를 `makeReady` 로 찍는다. `CREATE TABLE`
  SQL 은 한 글자도 안 바꿨다. 원래 이 코드는 `DATABASE_URL` 이 있을 때만
  돌아 자동 검증이 0이었는데, 규칙을 순수 함수로 떼어 내면서 **DB 없이도
  검증되는 자리**가 됐다. 179 → 184.

  테스트가 무는지도 확인했다 — 실패 무효화 한 줄을 일부러 지우니 **3개가
  빨간불**, 되돌리니 초록.

- **619건 중 4건의 좌표가 쓰레기인데 재는 쪽이 안 막고 있었다** —
  좌표가 비었거나 `(19.69, 117.99)` 기본값이 박힌 4건이 있다. 지도는
  `hasPlace` 로 걸러 그리고 있었지만 `coordsOf`·지역 축은 그대로 재서,
  **`서울 동작구` 로 진단하면 화면에 "직선거리 13317km"** 가 근거인 척 떴다
  (`null` 이 0 으로 읽혀 기니만 앞바다가 원점이 된다). `경기 양주시` 는 2268km.
  같은 술어를 재는 쪽에도 씌웠다 — `coordsOf` 는 쓸 만한 좌표만 고르고,
  상대 좌표가 불량이면 지역 축을 **빼고** 나머지로 잰다.

  **그 결과 게시 지표가 올라갔다** — 정밀도 60.2 → **61.8%**, 재현율
  54.8 → **55.5%**, 리프트 2.55 → **2.62배**. 모델을 고쳐서가 아니라
  쓰레기를 걸러서다. 없던 성능이 생긴 게 아니라 **있던 성능을 잘못 재고
  있었다.** 임계값도 실측을 따라 `0.27 → 0.24` 로 내렸다(3이웃 실측 최대가
  0.2674 → 0.2371 로 좁아졌다 — `match.test.ts` 가 그 규칙을 강제한다).

  `lib/festivals.test.ts` 를 새로 만들어 **데이터 자체를 검사**한다(건수·
  중복 id·필수 필드·축 범위·날짜·좌표 불량 건수). 194 통과.

**진짜 DB 를 확인하는 법 (로컬에는 DB 가 없다).**
`.env.local` 에 `DATABASE_URL` 이 없고 docker·psql·vercel CLI 도 없다.
그래서 **배포본이 곧 DB 검증 환경**이다. 쓰기 없이 세 테이블의 준비 경로를
다 지나가게 하는 지점은 이렇다.

| 확인 지점 | 지나가는 테이블 |
|---|---|
| `/` 의 표제란 "저장소" 칸이 `Postgres` 인가 | — (DB 경로를 타는지 자체) |
| `/` 가 200 이고 이력 건수가 뜨는가 | `entries` · `drafts` |
| `/venue?id=<없는 숫자>` 가 200 인가 | `venues` |

셋 다 읽기라 데이터를 건드리지 않는다. 캡처를 앞두고 이력은 0건으로 둔다.

---

## 지금 남은 것 — 서비스가 아니라 제출물이다

서비스 쪽은 끝났다. 1차 심사는 **서면 + 기능심사**이고, 서면이 통째로 비어 있다.

### 1. 캡처 4장을 다시 찍는다 ← 제일 먼저
`docs/제출양식/캡처/` 의 4장은 **8/30 14:55** 것이라 지금 화면과 다르다.
그 사이에 지도 아래 카드 넷이 생겼고, 감당 범위 문구가 바뀌었고, 시연 사례가
고령에서 횡성으로 옮겨졌다. **지금 제출하면 화면과 안 맞는 문서가 나간다.**
찍기 전에 배포본 이력을 0건으로 비워 시연 세트가 서게 할 것.

### 2. 기능설명서를 해시태그 넷에 맞춰 다시 쓴다
양식(`docs/제출양식/*.pptx`)과 작성본, `fill.py` 가 이미 있다. 뼈대는 있고
**내용을 해시태그에 매핑**하는 일이다.

| 해시태그 | 우리 기능 |
|---|---|
| `#축제/행사 기획` | 기획서 붙여넣기·PDF·TourAPI 검색·직접 입력 (5축) |
| `#사전 수요 예측` | **여기를 정면으로 쓴다** — 예측 대신 619건 실측 배수 경보 + 왜 예측하지 않는가 |
| `#보완 피드백 제공` | 감당 범위 · 행사장 도면 쏠림 스캔 · 진단서 A4 한 장 |
| `#데이터 기반` | 공사 OpenAPI 3종 + 데이터랩 619건 + 619건 자기검증 |

### 3. 발전성 20점을 화면 밖에서 건진다
검색량 축을 재고 버린 기록이 **가장 강한 발전성 서사**다("축을 늘리려다 우리
기준으로 기각했다"). 지금 `DECISIONS.md` 에만 있는데 심사위원은 그걸 안 읽는다.

### 사용자가 확인해 줘야 할 것
- **1차 심사자료 제출일** (공고문에 없다. 메모의 "9/21" 은 출처 미확인)
- ~~오픈라우터 키별 지출 상한~~ — **이미 걸려 있다.** 대시보드를 직접 열어
  확인했다(2026-08-31): `oneshot-mvp` 키에 **$1 TOTAL**, 사용액 `< $0.001`.
  2,000회분이라 심사 기간에 모자라지 않는다. 이 문서가 여러 회차째 "미결"이라고
  적어 온 것이 틀렸다. **남은 것은 같은 계정의 `Default key` 가 unlimited 인 것** —
  배포본이 그 키를 안 쓴다면 노출은 아니지만 안 쓰는 무제한 키는 열린 문이다.

### 열려 있는 판단 셋 (전부 사용자 결정)

- **LOO 60.2% 는 hold-out 이 없다.** 가중치·임계값을 이 619건으로 골랐다.
  지금은 화면 세 곳이 그 사실을 한 문장으로 밝히는 선에서 멈췄다. 진짜로 고치려면
  619건을 학습/시험으로 갈라야 하고, 그러면 표본이 줄어 숫자가 더 나빠진다.
- **`/report?entry=demo` 는 이력과 무관하게 늘 열린다.** 막지 않았다 — 시연에
  필요하다. 대신 종이가 견본임을 굵게 말하고 진단 시각을 따로 찍는다.
- **`app/venue/page.tsx` 의 중앙값 복제**가 아직 남아 있다(도면 동결 규칙 때문).
  동결을 풀 일이 있으면 `grade.ts` 의 정의를 쓰게 고칠 것.

### 하지 않기로 한 것
- **모바일** (사용자 배제, 2026-08-30)
- **행사장 도면 신규 기능** — 배치도 작성은 행사대행 용역 과업이다(해운대 모래축제 과업내용서 확인). 치수 입력·폴리곤 구역·배율 컨트롤·밑그림 강화는 안 만든다
- **물량 개수 출력** — `작년 물량 × 배수` 는 분모가 다른 두 비율을 섞는다. 619건이 반증했고 eval 09 를 깬다
- **TourAPI 검색을 '작년 축제 찾기'로 바꾸기** — 사용자 반려. `localBaseline` 이 이미 자동으로 집는다
- **검색량 화면 연결** — 재고 버렸다(2026-08-31). ρ 가 표본에 따라 0.22~0.34 로 널뛰고
  가장 큰 표본이 0.236 이라 컷 0.3 을 못 넘는다. 그리고 공사 API 가 아니라 배점 0
- **`findSimilar` 에 자기 제외 넣기** — 필요 없어졌다. 시연 사례를 619건과 겹치지
  않는 조건으로 옮겨서 화면 경로에서는 자기참조가 안 생긴다. `lib/eval.ts` 는
  자기 제외를 이미 자기 안에서 한다
- M3 점 보행 시뮬 · M4 3D 지도 · 자동 배치 생성(TestFit 류)

---

## 반드시 알아야 할 사실 (여기서 시간 날린다)

### 이 코드가 거짓말하기 쉬운 자리 (2026-08-31 적대적 검증에서 나온 것)

전부 같은 병이었다 — **재지 못한 것이 "안심" 쪽으로 기운다.** 새 기능을 붙일 때
"이 값이 결측이면 화면이 뭐라고 말하나"를 먼저 물을 것.

- **감당 범위 하한은 언제나 1.00 이다.** `localBaseline` 이 `matched` 안에서 기준을
  고르므로 `twinLo ≤ baseSurge` 가 늘 참이다(619건 중 기준이 있는 136건 전수 확인).
  측정값이 아니라 항등식이라 화면이 그렇게 말한다. **구간처럼 내지 말 것.**
- **`scanSeason` 은 부분 결측에서 거짓말한 적이 있다.** 12달 중 1달만 재도 `spread=0`
  이라 `flat` 이 참이 됐고 화면이 "시기는 갈리지 않습니다"라고 적었다. 지금은
  `measured` 를 싣고 `flat` 은 12달을 다 재야 참이 된다. `robust`·`monthMatchRate` 도
  같이 게이트했다. **셋 다 결측에 무방비였다 — 하나만 고치면 나머지가 거짓말한다.**
- **시연 사례는 619건과 겹치면 안 된다.** 옛 사례는 축제 등록값 그대로여서
  `findSimilar` 가 자기 자신을 거리 0.00 으로 집었다. `lib/demo.test.ts` 가 지금은
  "자기 자신을 안 집는다"를 못 박는다.
- **저장소의 시연 id 가로채기는 읽기 경로에만 있었다.** 쓰기 경로로 `"demo"` 가
  흘러 `Number("demo")=NaN` 이 BIGINT 에 들어가 배포본에서 100% 실패했다.
  `saveVenue` 에 숫자 가드가 있다. **읽기를 가로챘으면 쓰기도 봐야 한다.**
- **진단서 헤드라인과 목록이 다른 기준으로 정렬된다.** `competitorsNear` 는 거리순,
  `competitionHeadline` 은 배수 최대. 잘라 실으면 지목한 축제가 종이에서 빠진다.

### 데이터
- `actualVisitSurge` = 축제 기간 시군구 외지인 방문자 ÷ 평상시. **KT 이동통신 실측**(한국관광 데이터랩).
- 인구가 적을수록 배수가 커진다(`log(인구) vs 배수 r = -0.486`). 그래서 `lib/peer.ts` 가 또래 맥락을 낸다. **배수 자체는 보정하지 않는다.**
- 619건의 **76%가 배수 1.5 미만**(= 근거없음). 감당 범위가 대부분 안 뜨는 게 정상이다.

### TourAPI
- `contentid` **= 619건의 `id`**. 그래서 조인이 된다(2026-10 축제 140건 중 26건 일치).
- `searchFestival2` 에 **`areaCode` 를 주면 0건**. 좌표가 다 있으니 반경으로 거른다.
- **미래 축제는 등록이 거의 없다.** 같은 시기 경쟁은 *가장 최근에 지난 그 달*을 본다.
- 키는 `.env.local`·Vercel 에만. **e2e 는 "키 없음"이 전제**라 빈 문자열로 덮는다(`delete` 는 안 된다).

### 지도·도면
- 브이월드 백지도는 **z6~18**(z19 는 XML 오류). `tilemap.ZOOM_LIMITS` 에 실측으로 박아 뒀다.
- **크기의 진실은 미터다**(`KIND_SIZE_M`). 픽셀은 축척에서 나온다. 예전에 픽셀 고정이라 줌 16 에서 117m 부스가 생겼다 — 재발 방지 테스트 있음.
- 뷰 배율: `mPerPx = metersPerPixel(lat, zoom) / view`. 이 한 줄이 정의의 전부다. Konva Stage 는 **스케일하지 않는다**.
- `scaleItems`/`shiftItems` 는 `points` 있는 것(통로·**부지**)을 전부 다룬다.

### 작업 규약
- 새 의존성은 **추가 전 질문**. `any` 금지. 주석은 "왜"만. 문서·UI 문구는 한국어. 커밋은 기능 단위.
- 사이클: 순수 함수 TDD → 화면 → `npm test`·`typecheck`·`lint`·`build`·`test:e2e` → 크롬 확인 → 커밋 → main push(=Vercel 자동배포).
- **화면 확인**: `npm run build` 후 `DATABASE_URL= npx next start --port 3200`. 빌드 후 **서버 재시작 필수**.
  포트가 안 죽으면 PowerShell 로: `Get-NetTCPConnection -LocalPort 3200 -State Listen | %{ Stop-Process -Id $_.OwningProcess -Force }`
  이력은 e2e 의 `폼을낸다` 방식으로 서버 액션에 폼을 POST 해서 심는다.
- **크롬 자동화** — 버튼은 ref 클릭이 화면 배율 때문에 빗나간다. **JS `.click()` 이 확실하다.**
  스크린샷이 자주 타임아웃 난다(재시도하면 된다). 검증은 스크린샷 좌표가 아니라 **DOM 텍스트를 JS 로 읽어서** 한다.
  캡처는 `document.documentElement.style.zoom='0.62'` 로 줄이고 `scrollTo` 로 절대 위치를 잡으면 잘 나온다.
- **진단서·검증 보고서 인쇄 높이 재는 법** — 자동 검사가 없으니 손으로 잰다. 검증 보고서는 `.report-page` 마다 잰다(장당 273mm). 진단서를 열고
  CSS 파일의 `@media print` 규칙을 그대로 꺼내 화면에 적용한 뒤 높이를 mm 로 읽는다.
  ```js
  let pr=''; for(const sh of document.styleSheets){let rs;try{rs=[...sh.cssRules]}catch{continue}
   for(const r of rs) if(r.media&&[...r.media].join(',').includes('print'))
     pr+=[...r.cssRules].map(x=>x.cssText.replace(/;/g,' !important;')).join('\n');}
  const st=document.createElement('style');
  st.textContent='body{margin:0}\n.no-print{display:none!important}\n.report{width:186mm!important}\n'+pr;
  document.head.appendChild(st);
  // 가용 273mm 와 견준다
  document.querySelector('.report').getBoundingClientRect().height/(96/25.4)
  ```
  최악을 보려면 경쟁 목록 `li` 를 상한까지 복제하고 축제명을 한 줄 더 흐르게 늘린다.

### e2e 가 물고 있는 화면 계약 6개 (2026-08-31 구조 검증에서 나옴)

`app/` 하위에 유닛 테스트가 **0개**다. 3,468줄의 화면을 지키는 것은
`e2e.test.mjs` 8개뿐인데, 그 8개가 검사하는 것은 렌더된 **HTML 문자열**이다.
즉 아래 여섯은 사실상 **UI 의 공개 API** 인데 코드 어디에도 그렇게 적혀 있지 않다.
건드리면 e2e 가 **통과하면서** 화면이 깨진다 (거짓 초록).

| 표식 | 무엇에 물려 있나 | 깨지는 방식 |
|---|---|---|
| `$ACTION_ID_` **첫 등장 순서** | `e2e.test.mjs:69` 가 페이지의 첫 액션 id 를 잡는다 | `page.tsx` 의 JSX 순서를 바꾸면 추출 폼 대신 **삭제 폼**에 값이 간다. 검증이 `status < 500` 뿐이라 그래도 통과한다 |
| `class="map"` **개수 1** | `e2e.test.mjs:212` 가 개수까지 센다 | 클래스명 통일·CSS 모듈화 |
| `pin-card` · `pin-detail` · `evidence` | 문자열로 직접 센다 | 같은 위 |
| React 가 끼우는 `<!-- -->` **위치** | `진단 이력 (N건)` 정규식이 주석을 허용한다 | 컴포넌트를 쪼개면 텍스트 노드 경계가 바뀐다 |
| `/\d[\d,]*\s*명/` **0건** | 제품 불문율 1번을 재는 **유일한 자동 자** | 화면 문구를 재조립하면 예측 숫자가 조용히 새어 나간다 |
| 무JS 폼 POST 경로 | 상태가 URL 에만 살아야 성립 | `useState` 로 "개선"하면 e2e 가 이 화면을 더는 검증하지 못한다 |

**분해는 2026-08-31 에 했다.** 위 여섯을 개별로 못 박는 대신 **렌더된 HTML
전체**를 기준본으로 잡는 쪽을 택했다 — 표식 여섯을 하나씩 테스트로 옮기면
그 목록 자체가 또 손으로 유지하는 사본이 된다.

### 화면을 고치기 전에 — `evals/golden.mjs`

```
npm run build && node evals/golden.mjs before   # 고치기 전 12화면 저장
(리팩터)
npm run build && node evals/golden.mjs after    # 출력이 같은지
```

빌드 id·액션 해시·시각처럼 매번 바뀌는 값만 자리표시자로 바꾸고 **나머지는
바이트로 비교**한다. 액션 해시는 지우지 않고 자리표시자로 바꾸므로 개수와
등장 순서는 계속 비교된다 — e2e 가 기대는 "첫 액션이 추출 폼"이 그대로 지켜진다.

이 그물이 무는지도 확인해 두었다. 문구 한 단어를 바꾸면 `home-*` 6개만
걸리고 `venue`·`report` 는 그대로다. 처음에는 **모든 화면이 항상 다르다**고
나왔는데 원인이 Next 의 빌드 id 였다 — 정규화에 넣었다.

### 화면 문구 (2026-08-30 사용자 지적)
AI 티가 난다는 지적을 받고 걷어냈다. 새로 문구를 쓸 때 같은 함정을 피할 것.
- **em-dash(—) 남용 금지.** 끊어 쓴다.
- **한 문장에 `<strong>` 하나 이하.** 다 강조하면 아무것도 안 강조된다.
- 괄호 설명 겹치지 않기. 나열(A · B · C)을 문장으로 편다.

---

## 파일 지도

```
lib/
  match.ts      5축 유사도. 가중치 0.28/0.28/0.19/0.10/0.15(합 1) · 임계값 0.27
                테마 0.15 는 LOO 로 고른 값이다. 필수화(0.30)하면 오히려 나빠진다
  grade.ts      경보 등급(배수 중앙값). **중앙값 정의는 여기 하나뿐이어야 한다**
  eval.ts       자기검증 619건 LOO. LOO_PUBLISHED 를 테스트가 매번 재도출한다
  peer.ts       또래 비교 — 인구 구간별 상위 몇 %. 배수를 보정하지 않는다
  capacity.ts   감당 범위 — 상한 하나만 뜻이 있다. **하한은 항등식이라 늘 1.00**
                개수는 절대 안 낸다
  season.ts     시기 민감도 — 12달 재조회 + 쌍둥이 실제 개최월 + spread 기반 flat
                `measured` 로 부분 결측을 게이트한다 (flat 은 12달 다 재야 참)
  demo.ts       시연 세트 한 벌 (강원 횡성군·6월·음악·공연). 이력 0건일 때만 화면에
                선다. **619건 어느 축제와도 조건이 같으면 안 된다**
  preset.ts     예시 배치 200×140m. 크기는 전부 KIND_SIZE_M 에서 나온다
  extract.ts    기획서 → 5축 (LLM 1회). extractFailureMessage 가 실패 문구를 만든다
  overlap.ts    같은 시기 경쟁 — monthWindow·competitorsNear·competitionHeadline
  tourapi.ts    공사 OpenAPI. 순수 변환과 네트워크가 갈려 있다
  venue.ts      도면 도메인 — KIND_SIZE_M·polygonAreaM2·outsideSite·fitToSite·scaleItems
  scan.ts       쏠림 전수 스캔 (순수, LLM 0)
  searchvolume.ts 네이버 API HUB 검색어트렌드. **재고 버렸다 — 화면에 연결하지 말 것**
                  문이 셋이라 헷갈린다. 열쇠를 받을 수 있는 곳은 API HUB 뿐이다
  coastline.ts / mapproj.ts / store.ts / pdf.ts / festivals.ts / types.ts
app/
  page.tsx           진단 화면 (서버 컴포넌트, 선택 상태는 URL).
                     SeasonTable 과 TwinCards(지도 아래 넉 장)가 여기 있다
  peer-strip.tsx     또래 분포 띠 (인라인 SVG)
  twin-map.tsx       닮은 축제 지도
  report/page.tsx    진단서 A4
  venue/editor.tsx   Konva 편집기 (유일한 클라이언트 컴포넌트)
evals/
  cases.md           10줄 판정표. 09 가 제일 중요하다
  search-volume.mjs  검색량 상관 실험 (네이버 키 필요)
docs/
  DECISIONS.md       버린 길과 그 근거 — 두 번 파지 않으려고
  제출양식/          공모전 기능설명서 (지금은 손대지 않는다)
```
