# 인수인계 — 2026-08-30

> 새 세션은 이 파일부터 읽고, 이어서 **`docs/DECISIONS.md`** 를 읽는다.
> 이 파일은 "지금 무엇이 있나", DECISIONS 는 **"무엇을 안 하기로 했고 왜인가"** 다 — 버린 길을 두 번 파지 않으려고 나눠 뒀다.
> 코드 규약은 `CLAUDE.md`, 화면 구조는 `docs/wireframe.md`·`docs/screens.md`.

## 이 제품이 무엇인가

축제 기획안을 넣으면 **닮은 과거 축제 619건의 실측 방문 배수**로 위험 등급을 내고,
그 쏠림을 **행사장 도면 위에서 미리 잡아 보는** 웹서비스.

목표는 하나다 — 지자체 담당자가 **"왜 물량을 3배로 잡았습니까"에 댈 근거**를 갖는 것.
그래서 정확도가 아니라 **출처**가 값어치다.

- 배포: https://oneshot-mvp.vercel.app · 저장소: https://github.com/jangjunyong/oneshot-mvp
- 현재 `main` = `259b7cf`. 백업 브랜치 `backup/2026-08-30-야간작업` 있음. 브랜치 작업 없음(모두 main).
- 검증: 유닛 **155** + 무JS e2e 7 통과 (`npm test` · `npm run test:e2e`)

## 지금 겨누는 것 — 공모전

**2026 관광데이터 활용 공모전 ②-2 웹·앱 구현 부문, 지정과제 9번**
("축제 수요 예측 실패·주관적 기획 → 예산 낭비 / 대규모 쏠림 → 만족도 저하")

1차 심사 배점: 구현성 30 · 기획력 30 · **데이터 활용(공사 OpenAPI 필수) 20** · 발전성 20

- 수업 개별 과제는 **끝났다**(구글 시트 제출 완료). 이제 공모전만 본다.
- 기한 관리는 사용자 몫. 에이전트가 마감을 챙기지 않는다.
- 학습 목적 없음 — 설명보다 결과.

## 절대 어기지 않는 것 (제품 불문율)

1. **방문객 수를 예측하지 않는다.** 화면에 나가는 숫자는 과거가 실제로 겪은 *평소 대비 배수*뿐이다.
2. **"안전하다"고 말하지 않는다.** 근거를 못 찾은 것과 안전한 것은 다르다.
3. **왜 닮았는지를 항상 같이 낸다.** 유사도 점수만 던지면 실무자는 못 믿는다.
4. **출처 없는 숫자·그림을 화면에 올리지 않는다.** 못 구한 값은 비우고 "못 구했다"고 적는다.
5. **판정·시뮬에 LLM 0회.** 모델은 기획서에서 항목을 옮겨 적는 1회만.

## 오늘까지 만든 것

| 커밋 | 내용 |
|---|---|
| `e1f2493` | 지도 — 좌표 없는 4건 제외, 남한 실제 비율 캔버스, 겹치는 핀 비켜 세우기 |
| `1514486` | 지도는 페이지에 한 장, 핀 클릭 시 근거 (선택은 URL, 서버 렌더) |
| `2b702dc` | **해안선 테두리** — Natural Earth 10m(퍼블릭 도메인), 제주·울릉·독도·백령 |
| `3dfb8bf` | 도면 줌 제한을 타일 제공자 실측에 맞춤 (브이월드 6~18 / OSM·위성 5~19) |
| `7c43925` | **N1 같은 시기 경쟁** — 그 달 반경 50km 축제를 공사 OpenAPI 로 조회 |
| `2828dae` | **N2 대비 진단서** `/report?entry=<id>` A4 한 장 (+ 도면 재진입 버그 수정) |
| `0ea9404` | **N3 닮은 축제 등록 정보** — 개최기간·장소·주최·요금·홈페이지 |
| `21a2a50` | **도면이 실측이 됐다** — 미터 기반 크기 · 줌 18 · 겹침 방지 · 부지 경계 |
| `f79a9f5` | **부지에 맞춰 보기** — 뷰 배율(타일 오버줌)로 z18 너머를 당긴다 |
| `6b362a7` | **자기검증** — 619건 LOO 적중률을 화면·진단서에. 한계도 같이 |
| `d0f3861` | **감당 범위** — "왜 물량을 3배로" 에 배수 비율 구간으로 답한다 |
| `873e4fc` | **시기 민감도** — 표가 스스로 한계(요청월 일치 19%)를 말한다 |
| `1cbdde4` | **검색량 검증 인프라** — 키 게이트 + eval. 화면엔 미연결(검증 전) |
| `ddffd24` | **테마 가중치 0.10→0.15** — 감이 아니라 적중률(LOO)로 재보정 |
| `b1bf9fd` | 첫 화면 흐름도 3칸 · 결과 화면에서 감당 범위를 결론 자리로 |
| `926bd24` | **배수의 뜻 명시 + 또래 비교** — 인구 편향(r=-0.486)을 맥락으로 |
| `259b7cf` | 또래 분포 띠 · 위성 토글 제거 · **도면 예시 배치** |

> **2026-08-30 축 재조정.** 도면(`/venue`)은 **신규 기능을 안 만든다** — 배치도 작성은
> 대행사 과업이고 우리 사용자(담당 공무원)는 도면을 그리지 않는다. 그 자리를
> **자기검증 · 감당 범위 · 시기 민감도 · 또래 비교** 네 축이 대신한다.
> (사용자 요청으로 위성 토글 제거·예시 배치 추가는 예외로 했다.)
> 근거와 버린 길은 `docs/DECISIONS.md`.

### 화면 셋
- `/` 진단 — 기획서 붙여넣기/PDF/TourAPI 검색 → 5축 확정 → 닮은 축제 3곳 + 경보 등급 → 지도 한 장 + 같은 시기 경쟁 + **시기 민감도 + 감당 범위 + 자기검증**
- `/venue` 행사장 도면 — 브이월드 백지도 위 배치, 쏠림 전수 스캔, 부지 경계, 부지에 맞춰 보기 **(동결 — 신규 기능 안 만든다)**
- `/report` 진단서 — 결론 + **근거 5종** + 자기검증 + 한계 고지, 브라우저 인쇄로 A4 PDF

## 반드시 알아야 할 사실 (여기서 시간 날린다)

### TourAPI
- `contentid` **= 619건 데이터의 `id`** 다. 그래서 조인이 된다(2026-10 축제 140건 중 26건 일치).
- `searchFestival2` 에 **`areaCode` 를 주면 0건**이 온다. 좌표(mapx=경도, mapy=위도)가 다 있으니 반경으로 거른다.
- **미래 축제는 등록이 거의 없다**(2027-05 조회 0건). 그래서 같은 시기 경쟁은 *가장 최근에 지난 그 달*을 본다.
- `homepage` 는 `<a href=…>` 앵커로 올 때가 있다 → `homepageUrl()` 로 주소만 꺼낸다.
- 키는 `.env.local`·Vercel 에만 있다. **e2e 는 "키 없음"이 전제**라 빈 문자열로 덮는다(`delete` 로는 안 된다 — next start 가 도로 채운다).

### 지도·도면
- 브이월드 백지도는 **z6~18**(z19 는 XML 오류). OSM·Esri 위성은 z5~19. `tilemap.ZOOM_LIMITS` 에 실측으로 박아 뒀다.
- **크기의 진실은 미터다.** `lib/venue.ts` 의 `KIND_SIZE_M`. 픽셀은 축척(`mPerPx`)에서 나온다.
  예전에 픽셀 고정이라 줌 16 에서 **117m 부스**가 생겼다 — 재발 방지 테스트가 있다.
- 부지 경계는 `kind:"site"` 폴리곤 하나. 면적은 신발끈×축척², 경계 밖은 네 모서리 판정.
- **뷰 배율(`venue.map.view`)** — z18 위로 못 가는 몫을 타일 오버줌으로 채운다.
  `mPerPx = metersPerPixel(lat, zoom) / view` 가 정의의 전부다. 배율을 만질 때
  축척을 같은 곱으로 안 나누면 200m 부지가 545m 가 된다(테스트가 지킨다).
  Konva Stage 는 **스케일하지 않는다** — 도형 좌표가 곧 화면 픽셀이라
  포인터·Transformer·치수선·freeSpot 이 전부 무수정으로 산다.
- `scaleItems`/`shiftItems` 는 `points` 가 있는 것(통로·**부지**)을 전부 다룬다.
  예전 에디터 지역 함수는 `kind === "path"` 만 봐서 **지도를 끌면 부지 경계만
  제자리에 남았다** — 면적도 밖 판정도 통째로 거짓말이 되던 버그.

### 작업 규약
- 새 의존성은 **추가 전 질문**. `any` 금지. 주석은 "왜"만. 문서·UI 문구는 한국어. 커밋은 기능 단위.
- 사이클: 순수 함수 TDD → 화면 → `npm test`·`typecheck`·`lint`·`build`·`test:e2e` → 크롬으로 직접 확인 → 커밋 → main push(=Vercel 자동배포).
- **화면 확인 요령**: `npm run build` 후 `DATABASE_URL= npx next start --port 3200`.
  빌드 후에는 **서버를 반드시 재시작**해야 새 화면이 나온다. 진단 이력은 e2e 와 같은 방식으로 서버 액션에 폼을 POST 해서 심는다.
- 크롬 자동화: 팔레트·버튼은 **하이드레이션 후 다시 `find` 해서 얻은 ref** 로 눌러야 먹는다.
  캔버스는 **좌표 클릭·드래그가 먹는다**(CDP 실입력). 다만 화면 배율이 도중에 바뀌므로
  스크린샷 좌표를 믿지 말고, 검증은 우측 패널 숫자(면적·축척·실측)를 JS 로 읽어 한다.

## 다음 할 일 (우선순위)

1. **진단 화면 세로 길이** ← 바로 다음
   결론·또래·감당·근거 3종·시기 표 12행·자기검증이 한 줄로 쌓여 길다. 시기 표를 접을지 판단.
2. **모바일 미확인** — 시기 표 6열과 흐름도 3칸이 좁은 화면에서 어떻게 되는지 안 봤다.
3. **기능설명서 마무리** — `docs/기능설명서.md` 초안이 있다. **공모전 제출 양식을 받아 그 양식에 옮겨야 한다.**
   심사위원은 코드를 안 읽는다. 구현성 30 + 기획력 30 을 전달하는 건 문서다.
4. **시연 세트 고정** — 고령 대가야축제(경북/고령군/3월/테마3/인구3/접근성2)가 심각 등급 + 시기 표 + 감당 범위가 다 뜨는 사례다.
   도면까지 붙이려면 진단서 근거 3 이 빈칸이 되지 않게 시연용 도면 한 건을 미리 심어 둘 것.
5. **검색량 상관 검증** — 네이버 키를 넣고 `evals/search-volume.mjs`. |ρ| < 0.3 이면 그 축은 버린다.

### 하지 않기로 한 것
- **행사장 도면 신규 기능 (2026-08-30)** — 배치도 작성은 **행사대행 용역의 과업**이다(2026 해운대 모래축제 과업내용서 확인). 담당 공무원은 도면을 그리지 않고 받아서 심의에 올린다. 치수 입력·폴리곤 구역·배율 컨트롤·밑그림 강화는 만들지 않는다.
- **TourAPI 검색을 '작년 축제 찾기'로 바꾸기** — 2026-08-30 사용자 반려. "기획서 → 분석결과 구조에서 굳이 직접 넣는 불편함을 줄 이유가 없음". `localBaseline` 이 이미 자동으로 집는다.
- **물량 개수 출력** — `작년 물량 × 배수` 는 분모가 다른 두 비율을 섞는다. 619건이 반증했다(`docs/DECISIONS.md`). 그리고 `evals/cases.md` 09 를 깬다.
- **M3 점 보행 시뮬** — M2 정적 스캔이 이미 "어디가 막히나"를 답해 새 정보가 없고, 움직이는 점은 "예측"으로 읽혀 제품의 방어선을 흐린다.
- **M4 three.js 3D 지도** — 근거였던 "테두리 없고 흐릿함"이 해안선으로 해소됐다.
- **자동 배치 생성(TestFit 류)** — 우리 순서는 "사람이 그린 것을 검증"이 1순위.

## 파일 지도

```
lib/
  match.ts      5축 유사도·임계값 0.27(619건 LOO 실측 근거)·haversineKm
  grade.ts      경보 등급 (배수 중앙값). **중앙값 정의는 여기 하나뿐이어야 한다**
  eval.ts       자기검증 — 619건 LOO. LOO_PUBLISHED 를 테스트가 매번 재도출한다
  peer.ts       또래 비교 — 인구 구간별 분포에서 상위 몇 %. 배수를 보정하지 않는다
  preset.ts     예시 배치 200×140m — 지도 깔 때 같이 깔린다. 크기는 전부 실측
  capacity.ts   감당 범위 — 배수 비율 구간. **개수는 절대 안 낸다**
  season.ts     시기 민감도 — 12달 재조회 + 쌍둥이 실제 개최월 + spread 기반 flat
  searchvolume.ts 네이버 데이터랩 (키 게이트, 화면 미연결 — 검증 전)
  overlap.ts    같은 시기 경쟁 — monthWindow·competitorsNear·competitionHeadline
  tourapi.ts    공사 OpenAPI — 검색·기간조회·상세. 순수 변환(parseRegion 등)과 네트워크가 갈려 있다
  venue.ts      도면 도메인 — KIND_SIZE_M·sizeInPx·freeSpot·polygonAreaM2·outsideSite
                             ·fitToSite(부지에 맞춰 보기)·scaleItems·shiftItems·viewOf
  scan.ts       쏠림 전수 스캔 (순수, LLM 0)
  coastline.ts  해안선 53링 (Natural Earth)
  mapproj.ts    지도 투영 — 데이터를 import 하지 않는다(경계 테스트가 지킨다)
app/
  page.tsx           진단 화면 (서버 컴포넌트, 선택 상태는 URL)
  report/page.tsx    진단서 A4
  venue/editor.tsx   Konva 편집기 (유일한 클라이언트 컴포넌트)
```
