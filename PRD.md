# PRD — 분산한 줄 알았는데 왜 같이 떨어지나

> 한 페이지를 넘기지 않는다. 흐름·도구·실패 경로는 [`docs/FLOW.md`](docs/FLOW.md), 스택·규칙은 `CLAUDE.md`.

## 한 문장

**여러 종목에 나눠 담은 중장년 개인투자자**가 **계좌가 온통 파랗게 물든 걸 봤을 때**, 섹터를 나눴는데 왜 같이 떨어지는지 **확인할 수단이 없어서** 여전히 분산했다고 믿는데 — **공시를 뒤져 종목 사이를 잇는 회사를 찾아오는 에이전트**로부터 **"이 셋은 ○○를 거쳐 한 덩어리입니다"라는 답과 그 근거 그림**을 얻는다.

## 사용자

실제 이름 한 명. 페르소나 금지.

- **1차: 장준용의 아버지** — 분산투자 한다고 여러 섹터에 나눠 담았는데 계좌가 다 파란색이었다. 왜 같이 떨어지는지 모르고, 알아볼 방법도 모른다. **디지털 인터페이스가 어색하다.**
- **2차: 장준용** — 같은 문제를 겪지만 도구를 다룰 줄 안다

## 최근 사례

**2026년 8월, 아버지.** 계좌를 열어보고 *"분산투자 한다고 했는데 왜 다 파랗냐"* 고 했다.
이 한마디가 이 제품의 전부다. 아버지가 던진 질문은 "내 포트폴리오가 몇 덩어리냐"가 아니라 **"왜 같이 떨어지냐"** 였다. 증상이 먼저고 구조는 설명이다.

## 대체재

지금 쓰는 것: **증권사 앱**(섹터 비중 파이차트까지만) 과 **생성형 AI에 물어보기.**

Gemini로 실측했다 → [`docs/대체재_검증.md`](docs/대체재_검증.md)

**LLM은 이 문제를 상당 부분 푼다.** 6번 왕복이 걸렸지만 결국 정확한 답(다우데이타–다우기술–키움증권)을 냈다. *"AI로는 안 된다"* 는 주장은 성립하지 않고, 그걸 근거로 삼으면 안 된다.

무너지는 지점은 넷이고, 이 제품의 방어선은 앞의 둘이다.

| | LLM | 이 제품 |
|---|---|---|
| **① 검증** | 맞을 때와 틀릴 때 **말투가 같다**. 같은 대화에서 "KINDEX로 시작하는 주식(예: 한국토지신탁)" 같은 명백한 오류를 똑같이 단정적으로 말했다 | 관계마다 **보고서·연도·항목**. 없으면 안 그린다 |
| **② 완결성** | 모르는 관계를 **"없다"로** 답한다. "모르겠다"가 안 나온다 | **"관계 없음 + 확인한 범위"** 를 명시 |
| ③ 최신성 | 대량보유공시는 학습 데이터에 없다 | 런타임에 조회 |
| ④ 롱테일 | 유명 그룹은 잘 안다 | 코스피200 전체 + 온디맨드 |

> ⚠️ **④는 이 제품에 불리하게도 작동한다.** 아버지가 **유명 대기업만** 사셨다면 LLM으로 충분하고, 애초에 원인이 계열이 아니라 시장 전체일 수도 있다. 그러면 이 제품의 진단 자체가 틀린 설명이 된다.
> **→ 오늘 아버지께 실제 보유 종목 이름을 받아야 한다. 이게 아래 두 빈칸보다 더 결정적이다** — 저 둘은 제품의 형태를 정하지만 이건 **제품이 존재할 이유**를 정한다.

## 지불자와 사용자

**`________` ← 아버지께 직접 여쭙고 채울 것 (오늘)**

- 아버지가 **직접** 화면을 조작하시는가?
- 아니면 내가 대신 보고 **말로 설명**해 드리는가?

전자면 큰 글씨·최소 입력·결론 우선. 후자면 화면은 평범해도 되고 대신 출력이 **그대로 읽어드릴 수 있는 한 문장**이어야 한다. 완전히 다른 제품이다.
**잠정: 전자를 목표로 하되 첫 사용은 옆에서 같이 본다.** Day 7 사용자 테스트를 아버지께 그대로 할 수 있다.

## 현재 비용

**`________` ← 오늘 여쭐 것**

"그 계좌를 얼마나 자주 신경 쓰시는지, 확인하는 데 시간을 얼마나 쓰시는지."
답이 "별로 안 쓴다"여도 주제를 접지는 않는다. **중장년층은 이 비용을 시간이 아니라 손실로 치른다.** 계좌가 다 파란 게 그 비용이다. 다만 그 손실이 얼마였는지는 숫자로 들어야 한다.

## 쐐기

2주 안에 만들 딱 한 흐름 — **증상을 확인해 주고, 이유를 그림으로 보여준다.**

```
보유: 현대차 · 기아 · SK하이닉스

  "현대차와 기아는 한 덩어리입니다. 현대모비스를 거쳐 이어져 있어요."
   └ SK하이닉스는 따로입니다.

              [현대모비스]     ← 안 산 회사. 이게 둘을 잇는다
               ╱        ╲
         (현대차)      (기아)
         (SK하이닉스)          ← 사전 그래프엔 경로 없음
                                → 여기서부터 에이전트가 일한다
```

**왜 에이전트인가 · 루프 · 도구 6종 · 실패 경로는 [`docs/FLOW.md`](docs/FLOW.md)에 있다.** 요지만: 미리 구운 그래프는 원리적으로 불완전해서, 경로가 없을 때 *"관계가 없는 것"* 인지 *"내 그래프에 없을 뿐"* 인지 구분할 수 없다. 구분하려면 더 파야 하고 어디를 얼마나 팔지는 실행 전에 정할 수 없다.

### 화면 규칙

배경에 코스피200 전체를 깐다. **배경이 곧 논증이다** — 분산이 아니었다는 걸 보이려면 분산이 뭔지 보여줄 기준면이 있어야 한다. 다만 어지럽지 않게 하는 조건이 넷이다.

| 규칙 | 이유 |
|---|---|
| 배경 좌표는 **미리 계산해 고정** | 어지러움은 밀도가 아니라 **움직임**에서 온다. 실시간으로 자리잡으며 흔들리면 안 된다 |
| 배경은 **상호작용 없음** | 회색 노드는 클릭·호버·확대 전부 막는다. 잘못 눌렀을 때 뭔가 튀어나오는 게 제일 무섭다 |
| **결론 문장이 그래프 위에** | 그림은 근거, 문장이 답 |
| 색은 **채도가 아니라 명도**로 가름 | 중장년층 색각 저하는 채도 구분에서 먼저 온다 |

**용어**는 전부 일상어로. 클러스터 → **덩어리**, 노드·간선 → **회사·관계**, 경유 기업 → **다리 놓는 회사**.

| 관계 | 출처 (전부 DART 정형 API) |
|---|---|
| ⚫ 지배·계열 | 최대주주 현황 |
| 🟢 출자 | 타법인 출자현황 |
| 🔵 대량보유 | 대량보유상황보고 (에이전트가 런타임에 발견) |
| 🔴 같은 업종 | 기업개황 `induty_code` — 보여주되 **덩어리 판정에서는 뺀다** |

경로 탐색은 ⚫🟢🔵 위에서만, **최대 3홉**. 홉 제한이 없으면 대기업이 전부 한 덩어리가 되어 진단이 무의미해진다. 3홉이 맞는지는 Day 5~6에 실제 데이터로 조정한다.

### 연결은 두 축이다 — 섞지 않는다

아버지가 겪은 "다 같이 떨어짐"의 원인은 둘 중 하나거나 둘 다다. **진단이 다르므로 따로 낸다.**

| 축 | 뜻 | 어떻게 보여주나 | 아버지에게 할 말 |
|---|---|---|---|
| ⚫🟢 **지배구조** | 한 회사가 다른 회사를 지배 — **실적 연동** | **그래프 + 덩어리 판정** | "분산이 아니었습니다. 이 셋은 한 덩어리예요" |
| 🟣 **공동 대주주** | 같은 큰손(국민연금·외국계)이 둘 다 5% 이상 보유 — **수급 연동** | **그래프 아님. 한 줄 문장** | "분산은 맞는데, 5개 중 4개를 국민연금이 들고 있어서 그쪽이 팔면 같이 빠집니다" |

> ⚠️ 🟣를 그래프 간선으로 쓰면 안 된다. 국민연금은 대형주 대부분의 5% 이상 주주라 **국민연금을 거쳐 코스피 전체가 1홉으로 이어지고**, 모든 것을 잇는 간선은 아무것도 구분하지 못한다. 그래서 **덩어리 판정(연결요소)은 ⚫🟢로만** 하고 🟣는 별도 문장으로 낸다. (eval 11)
>
> 🟣의 데이터는 대량보유상황보고에 있다(국민연금·블랙록 등 5% 이상은 제출 의무). **다만 "외국인 지분율 전체"는 DART가 아니라 거래소 데이터라 범위 밖이다** — 5% 이상 대주주만 다룬다.

## 성공 판정

> **Day 14 데모에서 화면에 이것이 보인다** —
> 종목 5개를 넣으면 **"이 셋은 현대모비스를 거쳐 한 덩어리입니다"** 라는 문장이 먼저 뜨고, 그 아래 회색 배경 위에 내 종목과 다리 놓는 회사만 색으로 살아난 그림이 나온다.
> 그림 아래에 수급 축이 한 줄 붙는다 — **"그리고 5개 중 4개는 국민연금이 5% 이상 보유하고 있어, 그쪽이 팔면 함께 빠집니다."**
> 이어지지 않은 종목은 따로 떨어져 보인다. 관계를 누르면 **어느 공시 몇 년 어느 항목**에서 나온 건지 보인다.
> 못 찾으면 지어내지 않고 **"관계 없음 — 확인한 범위: ○○, ○○"** 라고 답한다.

기계 판정은 [`evals/cases.md`](evals/cases.md) 10줄로 전개했다. 실패해야 정상인 케이스가 5개다.

## 안 만들 것

- **로그인 · 회원가입 · 결제 · 관리자 · 알림 · 다크모드**
- **증권사 계좌 연동** — 종목은 직접 입력받는다
- **포트폴리오 저장** — 세션 안에서만. DB 없음
- **시세 · 수익률 · 차트** — 관계를 보는 도구지 시세 앱이 아니다
- **투자 추천 · 매수매도 의견** — 진단까지만. 판단은 사용자 몫
- **코스피200 밖 사전 수집** — 온디맨드로만
- **사업보고서 본문 파싱** — 정형 API로 안 되는 건 2주 안에 안 한다

## 암묵지

여덟 번째 질문의 답 셋. 남에게 설명한 적 없는 규칙들.

1. **근거 없는 관계는 그리지 않는다.** 모델이 "아마 이어져 있을 것"이라고 판단해도 공시 항목으로 짚을 수 없으면 화면에 안 나온다. 추측을 그리는 순간 이 제품의 가치는 0이다.
2. **🔴 같은 업종은 보여주되 판정에서 뺀다.** 업종으로 묶으면 "반도체 둘이 한 덩어리"라는 당연한 소리만 나온다.
3. **"관계 없음"은 실패가 아니라 결과다.** 못 찾았을 때 확인한 범위를 같이 내놓는 것이 답이다. 빈 화면은 답이 아니다.
