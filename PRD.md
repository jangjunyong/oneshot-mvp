# PRD — 왜 다 같이 떨어졌는지 찾아주는 에이전트

> 한 페이지를 넘기지 않는다. 루프·도구·실패 경로는 [`docs/FLOW.md`](docs/FLOW.md), 스택·규칙은 `CLAUDE.md`.

## 한 문장

**여러 종목에 나눠 담은 중장년 개인투자자**가 **계좌가 온통 파랗게 물든 걸 봤을 때**, 분산했는데 왜 다 같이 떨어졌는지 **알아낼 방법이 없어서** 이유도 모른 채 불안해하는데 — **가설을 하나씩 공시로 검증하는 에이전트**로부터 **"이래서 같이 떨어진 겁니다"라는 원인 한 문장과 그 근거**를 얻는다.

## 사용자

실제 이름 한 명. 페르소나 금지.

- **1차: 장준용의 아버지** — 분산투자 한다고 여러 섹터에 나눠 담았는데 계좌가 다 파란색이었다. **왜 그런지 모른다.** 디지털 인터페이스가 어색하다.
- **2차: 장준용** — 같은 문제를 겪지만 도구를 다룰 줄 안다

## 최근 사례

**2026년 8월, 아버지.** 계좌를 열어보고 *"분산투자 한다고 했는데 왜 다 파랗냐"* 고 했다.

이 질문이 제품의 전부다. **아버지가 물은 건 "내 포트폴리오 구조가 어떻게 되냐"가 아니라 "왜"다.** 그래서 이 제품은 구조를 그려주는 도구가 아니라 **원인을 찾아주는 도구**다.

## 대체재

지금 쓰는 것: **증권사 앱**(섹터 비중 파이차트까지) 과 **생성형 AI에 물어보기.**

Gemini로 실측했다 → [`docs/대체재_검증.md`](docs/대체재_검증.md)

**LLM은 이 문제를 상당 부분 푼다.** 6번 왕복이 걸렸지만 결국 정확한 답을 냈다. *"AI로는 안 된다"* 는 성립하지 않고, 그걸 근거로 삼으면 발표에서 바로 무너진다. 무너지는 지점은 넷이고 방어선은 앞의 둘이다.

| | LLM | 이 제품 |
|---|---|---|
| **① 검증** | 맞을 때와 틀릴 때 **말투가 같다**. 같은 대화에서 "KINDEX로 시작하는 주식(예: 한국토지신탁)" 같은 오류를 똑같이 단정적으로 말했다 | 근거마다 **보고서·연도·항목**. 없으면 주장하지 않는다 |
| **② 완결성** | 모르는 걸 **"없다"로** 답한다. "모르겠다"가 안 나온다 | **"확인한 범위"** 를 같이 낸다 |
| ③ 최신성 | 대량보유공시는 학습 데이터에 없다 | 런타임에 조회 |
| ④ 롱테일 | 유명 그룹은 잘 안다 | 코스피200 + 온디맨드 |

> ⚠️ **④는 이 제품에 불리하게도 작동한다.** 아버지가 유명 대기업만 사셨다면 LLM으로 충분하다.
> **→ 오늘 실제 보유 종목 이름을 받아야 한다. 아래 두 빈칸보다 결정적이다.**

## 지불자와 사용자

**`________` ← 아버지께 여쭙고 채울 것 (오늘)**
직접 조작하시는가, 내가 대신 보고 말로 전해 드리는가. 전자면 큰 글씨·최소 입력, 후자면 **출력이 그대로 읽어드릴 수 있는 한 문장**이어야 한다.
**잠정: 전자를 목표로 하되 첫 사용은 옆에서 함께.**

## 현재 비용

**`________` ← 오늘 여쭐 것**
얼마나 자주 신경 쓰시는지, 확인에 시간을 얼마나 쓰시는지. **중장년층은 이 비용을 시간이 아니라 손실로 치른다.**

## 쐐기

2주 안에 만들 딱 한 흐름 — **가설을 하나씩 검증해서 원인을 찾는다.**

종목을 넣으면 에이전트가 "왜 같이 움직였나"를 놓고 가설을 세우고, 공시로 하나씩 확인한다.

| 가설 | 뜻 | 확인 방법 | 나오는 답 |
|---|---|---|---|
| **1. 지배구조** | 한 회사가 다른 회사를 지배 — 실적 연동 | 최대주주현황 · 타법인출자 | "**분산이 아니었습니다.** 이 셋은 현대모비스를 거쳐 한 덩어리예요" |
| **2. 공동 대주주** | 같은 큰손이 둘 다 5% 이상 — 수급 연동 | 대량보유상황보고 | "분산은 맞는데, 5개 중 4개를 국민연금이 들고 있어 그쪽이 팔면 같이 빠집니다" |
| **3. 같은 업종** | 업종 동조 | 기업개황 `induty_code` | "업종이 겹칩니다. 반도체 셋은 같이 움직여요" |
| **4. 시장 전체** | 그냥 코스피가 빠짐 | 지수 대비 등락 (슬라이스 4) | "**분산 문제가 아닙니다.** 시장 전체가 빠졌어요" |

**4번이 나올 수 있어야 이 제품을 믿을 수 있다.** *"당신 잘못이 아니라 시장이 빠진 겁니다"* 가 정직한 답인 경우가 실제로 많다. 무조건 "계열 때문"이라고 답하는 도구는 틀린 도구다.

### 왜 에이전트인가

**어느 가설부터 볼지, 몇 개나 볼지, 언제 "이게 원인이다"라고 할지를 모델이 정한다.**

가설 1에서 강한 증거가 나오면 나머지를 안 본다. 약하면 더 판다. 종목이 3개면 두 가설로 끝나고 8개면 조합이 많아 더 봐야 한다. **첫 조회 결과가 다음 행동을 정하므로 실행 전에 호출 순서를 그릴 수 없다.**

루프·도구·실패 경로·판정 근거는 [`docs/FLOW.md`](docs/FLOW.md).

### 화면 — 답이 주인공, 그래프는 근거

```
┌──────────────────────────────────────────┐
│  현대차·기아가 한 덩어리라서 같이 떨어졌습니다.  │ ← 답. 제일 큼
│  현대모비스를 통해 이어져 있어요.               │
│                                            │
│  ▸ 근거 보기          [최대주주현황 2025]     │
│  ┌────────────────────────────┐            │
│  │   (관계 그림 — 접혀 있음)      │            │ ← 사이드
│  └────────────────────────────┘            │
│                                            │
│  + 5개 중 4개를 국민연금이 5% 이상 보유합니다   │ ← 가설2
│  + 시장 전체는 같은 기간 -3% 였습니다          │ ← 가설4
└──────────────────────────────────────────┘
```

- **그래프는 기본 접혀 있다.** "근거 보기"를 눌러야 펼쳐진다
- 펼치면 코스피200 배경 위에 내 종목과 다리 놓는 회사만 색으로 살아난다 — **배경이 곧 논증이다.** 분산이 아니었다는 걸 보이려면 분산이 뭔지 보여줄 기준면이 필요하다
- 배경은 **좌표 고정 · 상호작용 없음.** 어지러움은 밀도가 아니라 움직임에서 온다
- 배경 노드는 클릭·호버·확대 전부 막는다. 잘못 눌렀을 때 뭔가 튀어나오는 게 제일 무섭다
- 색은 **채도가 아니라 명도**로 가른다 (중장년층 색각 저하는 채도에서 먼저 온다)
- 용어는 일상어 — 클러스터→**덩어리**, 노드·간선→**회사·관계**, 경유 기업→**다리 놓는 회사**

경로 탐색은 지배·출자 관계 위에서만 **최대 3홉**. 제한이 없으면 대기업이 전부 한 덩어리가 되어 진단이 무의미해진다.

> ⚠️ **기관투자자는 그래프 노드에서 뺀다.** 국민연금은 대형주 대부분의 5% 이상 주주라 간선으로 쓰면 **국민연금을 거쳐 코스피 전체가 1홉으로 이어진다.** 모든 것을 잇는 간선은 아무것도 구분하지 못한다. 그래서 가설 2는 그래프가 아니라 **문장**으로 낸다.

## 성공 판정

> **Day 14 데모에서 화면에 이것이 보인다** —
> 종목 5개를 넣으면 에이전트가 가설을 검증하는 로그가 흐르고, **"현대차와 기아가 한 덩어리라서 같이 떨어졌습니다"** 같은 **원인 한 문장**이 크게 뜬다.
> "근거 보기"를 누르면 관계 그림이 펼쳐지고, 관계를 누르면 **어느 공시 몇 년 어느 항목**인지 보인다.
> 서로 무관한 종목만 넣으면 지어내지 않고 **"지배구조로는 안 이어집니다 — 확인한 범위: ○○, ○○"** 라고 답한다.

기계 판정은 [`evals/cases.md`](evals/cases.md)로 전개했다.

## 안 만들 것

- **로그인 · 회원가입 · 결제 · 관리자 · 알림 · 다크모드**
- **증권사 계좌 연동** — 종목은 직접 입력받는다
- **포트폴리오 저장** — 세션 안에서만. DB 없음
- **차트 · 수익률 · 실시간 시세** — 가설 4를 위한 **기간 등락률 한 줄**만 예외. 그 이상은 안 한다
- **투자 추천 · 매수매도 의견** — 원인 설명까지만. 판단은 사용자 몫
- **코스피200 밖 사전 수집** — 온디맨드로만
- **사업보고서 본문 파싱** — 정형 API로 안 되는 건 2주 안에 안 한다

## 암묵지

여덟 번째 질문의 답 셋.

1. **근거 없는 원인은 말하지 않는다.** 모델이 "아마 계열일 것"이라고 판단해도 공시 항목으로 짚을 수 없으면 화면에 안 나온다. 추측을 단정하는 순간 이 제품은 LLM과 똑같아진다.
2. **"계열 때문"으로 몰지 않는다.** 시장 전체 하락이 답이면 그렇게 말해야 한다. 항상 같은 원인을 대는 진단기는 진단기가 아니다.
3. **"원인 못 찾음"은 실패가 아니라 결과다.** 확인한 범위를 같이 내놓는 것이 답이다. 빈 화면은 답이 아니다.
