# PRD — 공시 근거로 보유 종목 관계를 캐는 에이전트

> 한 페이지를 넘기지 않는다. 스택·폴더구조·UI 규칙은 여기 쓰지 않고 `CLAUDE.md`로 간다.

## 한 문장

**주식을 3종목 이상 보유한 개인투자자**가 **포트폴리오를 점검할 때**, 서로 다른 섹터에 나눠 담았어도 그 종목들이 **어느 회사를 경유해 한 덩어리로 이어지는지 확인할 수단이 없어서** 분산했다고 착각하는데 — **공시를 직접 뒤져 경로를 찾아오는 에이전트**로부터 **경유 기업까지 그려진 관계 그래프와 "실질 독립 클러스터 N개" 진단**을 얻는다.

## 사용자

실제 이름 한 명. 페르소나 금지.

- **1차: 장준용** — 여러 섹터에 나눠 담았는데 그게 실제로 분산인지 확인할 방법이 없었다. 지금은 네이버 금융에서 종목을 하나씩 열어보고 머릿속으로 짐작하는 게 전부.
- **2차 확인 대상 (오늘 안에 채울 것):** `________` — 3종목 이상 보유한 사람 1명에게 이 PRD를 보여주고 "이거 필요하냐"를 직접 듣는다. 강의 Day 1 요구사항이다.

## 대체재

지금 그 사람이 쓰고 있는 것: **아무것도 안 쓴다.**

굳이 한다면 종목을 하나씩 열어 최대주주란을 눈으로 훑고 머릿속으로 짐작한다. 증권사 앱은 섹터 비중 파이차트까지만 보여주는데, **섹터가 달라도 지배구조가 같으면 분산이 아니라는 사실은 어디에도 안 나온다.**

> ⚠️ **리스크로 정직하게 적어둔다.** 강의는 *"02에서 '아무것도 안 함'이 나오면 대개 문제가 아닙니다"* 라고 경고했다. 여기서의 반론은 **필요가 없어서가 아니라 수단이 없어서**라는 것이다. 맞는지는 위 2차 확인 대상에게 오늘 검증한다. 틀리면 주제를 바꾼다.

## 쐐기

2주 안에 만들 딱 한 흐름 — **에이전트가 공시를 뒤져 경로를 찾아오고, 그 결과로 그래프가 자란다.**

```
보유: 현대차 · 기아 · SK하이닉스

              [현대모비스]          ← 안 산 회사. 이게 둘을 잇는다
               ╱        ╲
         (현대차)      (기아)        ← 한 덩어리

         (SK하이닉스)                ← 사전 그래프엔 경로 없음
                                       → 에이전트가 여기서부터 일한다
```

### 왜 에이전트인가

사전에 구운 그래프는 **원리적으로 불완전하다.** 이유는 셋이고 전부 실제다.

1. 정형 API에 안 잡히는 관계가 있다 (계열회사 현황, 특수관계인)
2. 코스피200 밖 종목은 아예 없다
3. 구운 날짜의 스냅샷이라 최근 지분 변동이 빠져 있다

그래서 경로가 안 나왔을 때 **"관계가 없는 것"인지 "내 그래프에 없을 뿐"인지 구분할 수 없다.** 구분하려면 더 파야 하는데, **어디를 얼마나 팔지는 실행 전에 정할 수 없다.** 들어온 종목이 무엇이냐, 사전 그래프에 있느냐, 첫 조회에서 뭐가 나오느냐에 따라 매번 달라진다.

실행 전에 도구 호출 순서를 그릴 수 없다 → 강의 판정표의 **에이전트** 칸.

### 루프

```
경로 탐색 → 없음
  ↓ 모델 판단: 멈출까, 더 팔까? 어디를?
  ├ 대량보유 상황보고를 조회한다
  ├ 커버리지 밖이면 corp_code를 찾아 온디맨드 수집
  └ 최대주주 변동 이력을 연다
  ↓ 새 간선 발견 → 그래프에 추가 → 다시 탐색
  ↓ 스텝 소진 or 근거 없음 → "관계 없음 (확인 범위: ___)"
```

| | |
|---|---|
| **목표** | 보유 종목이 실제로 몇 덩어리인지, 무엇을 경유해 이어지는지 |
| **도구** | 기업개황 · 공시검색 · 최대주주현황 · 타법인출자현황 · 대량보유상황보고 · 임원주요주주소유보고 (전부 DART 정형 API) |
| **검증** | 알려진 지배구조 5쌍이 잡히는가 · 근거 없는 간선 0개 · `evals/cases.md` |
| **경계** | 근거 없으면 간선을 만들지 않는다 (규칙이지 판단이 아니다) · 최대 스텝 수 · 방문 `corp_code` 집합 |

### 화면에 뜨는 것

1. **내 보유 종목** — 강조 표시
2. **경유 기업** — 내가 안 샀지만 보유 종목 둘을 잇는 회사. 이 제품의 주인공이다
3. **간선** — 클릭하면 공시 출처가 뜬다
4. **에이전트 작업 로그** — *"SK하이닉스의 대량보유 공시를 확인합니다"* 처럼 무엇을 왜 열었는지 한 줄씩. **에이전트가 일하는 게 보이는 곳이다**

| 간선 | 출처 (전부 DART 정형 API) | 성격 |
|---|---|---|
| ⚫ 지배·계열 | 최대주주 현황 | 사실 |
| 🟢 출자 | 타법인 출자현황 | 사실 |
| 🔵 대량보유 | 주식등의 대량보유상황보고 | 사실 (에이전트가 런타임에 발견) |
| 🔴 동일 산업 | 기업개황 `induty_code` | 사실 (판정에서는 제외) |

### 사전 / 온디맨드 분리

- **사전(배치):** 코스피200의 ⚫🟢 간선만 구워 `graph.json`으로 둔다
- **온디맨드(에이전트):** 나머지 전부. 코스피200 안 종목끼리는 즉답, 밖이거나 경로가 없을 때 에이전트가 작동한다
- 사전 그래프가 너무 완벽하면 에이전트가 할 일이 없어 보이고, 너무 비면 매번 느리다. 이 경계가 이 제품의 튜닝 지점이다

### 범위 규칙

- **커버리지는 코스피200 전 섹터.** 분산은 섹터를 건너뛰는 문제라서 한 섹터로 자르면 진단 자체가 무의미해진다
- **기본 화면은 코스피200 전체 그래프.** 내 보유 종목과 경유 기업만 색으로 살리고 나머지는 흐리게 깐다. 배경이 빽빽해야 대비로 드러난다
- **경로 탐색은 ⚫🟢🔵 위에서만, 최대 3홉.** 홉 제한이 없으면 대기업이 전부 한 덩어리로 이어져 진단이 무의미해진다. 3홉이 맞는 값인지는 Day 5~6에 실제 데이터로 조정한다
- **클러스터 판정에서 🔴은 제외한다.** 동일 산업으로 묶으면 "반도체 2종목이 한 덩어리"라는 당연한 소리만 나온다

## 성공 판정

> **Day 14 데모에서 화면에 이것이 보인다** —
> 서로 다른 섹터의 종목 5개를 넣으면 사전 그래프에서 일부가 즉시 이어지고, **이어지지 않은 종목에 대해 에이전트가 "○○의 대량보유 공시를 확인합니다" 같은 로그를 남기며 공시를 열어보고, 새 간선이 화면에 추가되어 그래프가 자란다.**
> 아래에 **"실질 독립 클러스터 3개 — 현대차와 기아가 현대모비스를 통해 한 덩어리입니다"** 같은 진단 문장이 뜨고, 간선을 클릭하면 그 관계의 **공시 출처(보고서·연도·필드)** 가 보인다.
> 끝까지 못 찾으면 지어내지 않고 **"관계 없음 — 확인한 범위: ○○, ○○"** 라고 답한다.

기계로 판정할 것은 `evals/cases.md`로 전개한다.

## 안 만들 것

← 이 섹션이 없으면 PRD가 아니다.

- **로그인 · 회원가입 · 결제 · 관리자 · 알림 · 다크모드**
- **포트폴리오 저장** — 세션 안에서만. DB 없음
- **시세 · 수익률 · 차트** — 관계 그래프지 시세 앱이 아니다
- **투자 추천 · 매수매도 의견** — 진단까지만. 판단은 사용자 몫
- **모바일 전용 레이아웃** — 반응형까지만
- **코스피200 밖 사전 수집** — 온디맨드로만
- **사업보고서 본문 파싱** — 정형 API로 안 되는 건 2주 안에 안 한다

## 암묵지

여덟 번째 질문의 답 세 개. 남에게 설명한 적 없는 규칙들.

1. **근거 없는 간선은 그리지 않는다.** 모델이 "아마 관계가 있을 것"이라고 판단해도 공시 필드로 짚을 수 없으면 화면에 안 나온다. 추측을 그리는 순간 이 제품의 가치는 0이다.
2. **🔴 동일산업은 보여주되 판정에서 뺀다.** 눈에는 보여야 맥락이 잡히지만, 클러스터 계산에 넣으면 당연한 소리만 나온다.
3. **"관계 없음"은 실패가 아니라 결과다.** 못 찾았을 때 확인한 범위를 같이 내놓는 것이 답이다. 빈 화면은 답이 아니다.
